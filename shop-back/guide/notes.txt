# Creating a new project
nest new <name of project>

# Creating a new module
nest g module <name of module>

# Creating a new controller
nest g controller <name of controller>

# Creating a new service
nest g service <name of service>

# Installing typeOrm 
npm install @nestjs/typeorm typeorm

# Validation packages
npm install class-validator class-transformer


Action,Old Way (npm),New Way (Bun)
Install,npm install,bun install
Add Package,npm install @nestjs/config,bun add @nestjs/config
Run Dev,npm run start:dev,bun dev
Generate,nest g service users,bunx nest g service users


# Sqlite commands
.tables ->  List all tables
.schema table_name -> Show table schema

.headers on  -> Show column headers
.mode column  -> Format output in columns
SELECT * FROM table_name LIMIT 10;


# postgresql
# Connect to your database
psql -U jeff -d start_nest_shop_db -h localhost
# Enter your password when prompted
-- List all tables in the current database
\dt

-- List all tables with more details (including size, description)
\dt+

-- List all relations (tables, views, sequences, etc.)
\z

-- Describe a specific table structure
\d "order"        -- Note: quotes needed for uppercase/camelCase names
\d role
\d order_line_item

-- Or use snake_case if your tables use that naming
\d order_line_item-- 

--Show all tables in the public schema
SELECT table_name FROM information_schema.tables 
WHERE table_schema = 'public' 
AND table_type = 'BASE TABLE';

-- Exit psql
\q