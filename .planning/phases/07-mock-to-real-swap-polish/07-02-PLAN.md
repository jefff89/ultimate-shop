---
phase: 07-mock-to-real-swap-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - .planning/phases/07-mock-to-real-swap-polish/07-02-SUMMARY.md
autonomous: false
requirements:
  - SC2
  - SC4
user_setup: []

must_haves:
  truths:
    - "The landing page loads, scrolls, and paginates against the real GET /products with real DB product data"
    - "On a hard reload, page 1 is served from the SSR-hydrated cache — exactly one no-cursor GET /products fires server-side and zero from the browser on first paint (SC2)"
    - "Scrolling fires GET /products with a cursor exactly once per boundary and the feed rails fill from the real endpoints"
    - "EXPLAIN ANALYZE on the seeded keyset query uses the composite (isActive, createdAt, id) index, not a Seq Scan (SC4)"
    - "Lighthouse (or web-vitals onCLS) reports CLS <= 0.1 on the landing page (SC4)"
    - "The real-source Zod egress .parse() catches no contract drift on live data (SC4)"
  artifacts:
    - path: ".planning/phases/07-mock-to-real-swap-polish/07-02-SUMMARY.md"
      provides: "Recorded evidence for all four E2E verification gates (dedup, scroll/paginate, index, CLS) + the live Zod drift result"
      contains: "Index Scan"
  key_links:
    - from: "Browser landing page (:3001)"
      to: "GET /products (:3002 via Start proxy)"
      via: "useInfiniteQuery -> seam -> createServerFn"
      pattern: "products"
    - from: "GET /products keyset query"
      to: "composite index IDX_ad6840fea7aa63e7fad036ae6a"
      via: "Postgres planner Index Scan Backward on seeded data"
      pattern: "Index Scan"
---

<objective>
Prove the post-swap stack works end-to-end against the Phase 7 pitfall checklist. Plan 07-01 built and flipped the seam and seeded the data; this plan RUNS the integration with both servers live and verifies the four behaviors that cannot be unit-tested: (1) the landing page loads/scrolls/paginates on real data with NO duplicate page-1 request on hard load (SC2), (2) the composite keyset index is actually used now that the table is seeded (SC4), (3) Lighthouse/web-vitals CLS <= 0.1 (SC4), and (4) the live response passes the Zod egress gate with no contract drift (SC4).

Purpose: This is the milestone's final acceptance. Each gate here corresponds to a documented pitfall (Seq Scan on tiny tables, duplicate page-1 on hydration, layout shift). The checks require a browser DevTools/Network inspection, a live `EXPLAIN ANALYZE`, and a Lighthouse run — work Claude automates as far as the CLI allows, then a human confirms the browser-observable results.

Output: `07-02-SUMMARY.md` recording the evidence (Network screenshots/notes, the EXPLAIN ANALYZE plan output, the CLS number) for each gate.

Depends on 07-01: the seam must point at the real source AND the DB must be seeded (200+ rows) before any of these gates can pass — a 1-row table forces a Seq Scan (false index failure) and an unseeded grid cannot scroll.
</objective>

<execution_context>
@/home/jeff/Documents/start-nest-shop/.claude/get-shit-done/workflows/execute-plan.md
@/home/jeff/Documents/start-nest-shop/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/07-mock-to-real-swap-polish/07-RESEARCH.md
@.planning/phases/07-mock-to-real-swap-polish/07-VALIDATION.md
@.planning/phases/07-mock-to-real-swap-polish/07-01-SUMMARY.md
</context>

## Phase Goal

**As a** shopper, **I want to** browse the live catalog on the homepage, **so that** I see and scroll through real products and rails served by the backend instead of mock data.

> Note: ROADMAP `**Goal:**` line for Phase 7 is an integration outcome, not a canonical user story; the story above is derived from it plus the project core value (see 07-01-PLAN.md). SC3 (scroll restoration on back-navigation) is DROPPED from this phase per the user decision and is intentionally absent from the gates below.

## Artifacts this phase produces (Plan 07-02)

- `.planning/phases/07-mock-to-real-swap-polish/07-02-SUMMARY.md` — the verification evidence record (no source code; this is a verification plan). It captures the EXPLAIN ANALYZE plan line, the page-1 dedup observation, the CLS value, and the live Zod parse result.

<tasks>

<task type="auto">
  <name>Task 1: Bring up the live stack, seed-confirm, and verify the keyset index is used (EXPLAIN ANALYZE)</name>
  <files>(no source files — runs the stack and the DB query, records output to the SUMMARY)</files>
  <read_first>
    - 07-RESEARCH.md "EXPLAIN ANALYZE on the keyset query (after seeding)" code block and Pitfall 1 (Seq Scan on tiny tables is a false failure; expect `Index Scan Backward using IDX_ad6840fea7aa63e7fad036ae6a` on seeded data)
    - shop-back/src/products/products.service.ts findCatalogPage (the exact query: WHERE isActive = true AND PRICE_EXPR IS NOT NULL [AND (createdAt,id) < (:cAt,:cId)] ORDER BY createdAt DESC, id DESC LIMIT size+1)
    - 07-01-SUMMARY.md (confirm the seed ran and the active row count, and the seed script name)
    - CLAUDE.md (backend dev: `cd shop-back && bun run start:dev` on :3002, Postgres must be running; frontend dev: `cd shop-front && bun --bun run dev` on :3001; API_URL=http://localhost:3002)
  </read_first>
  <action>
    Confirm prerequisites: Postgres is up and the seed populated >= 200 active rows (re-run the 07-01 seed script if the count is low). Start the backend (`cd shop-back && bun run start:dev`, port 3002) — run it in the background so it stays up for Task 2.
    Run `EXPLAIN ANALYZE` against the seeded keyset query using the form in 07-RESEARCH.md: select the 9 card columns + createdAt FROM product WHERE "isActive" = true AND the price-presence predicate, with a representative cursor predicate `(product."createdAt", product.id) < (<a real seeded createdAt>, <a real seeded id>)`, ORDER BY "createdAt" DESC, id DESC LIMIT 25. Pick a real (createdAt, id) tuple from the seeded data (e.g. `SELECT "createdAt", id FROM product WHERE "isActive" = true ORDER BY "createdAt" DESC, id DESC LIMIT 1 OFFSET 50`) so the planner sees a mid-table seek.
    Read the plan output: confirm it shows an Index Scan (Backward) using the composite index `IDX_ad6840fea7aa63e7fad036ae6a` (the `(isActive, createdAt, id)` index), NOT a `Seq Scan on product`. If it shows a Seq Scan, first confirm the row count is genuinely >= 200 (Pitfall 1); only as a usability confirmation may you run `SET enable_seqscan = off;` then re-EXPLAIN to prove the index is usable — but the real pass is the planner choosing it on seeded data. Record the exact plan line(s) in the SUMMARY.
  </action>
  <verify>
    <automated>cd /home/jeff/Documents/start-nest-shop && PGPASSWORD="$DB_PASS" psql -h "${DB_HOST:-localhost}" -U "${DB_USER:-jeff}" -d "${DB_NAME:-start_nest_shop_db}" -c "EXPLAIN ANALYZE SELECT product.id, product.name, product.slug, product.\"primaryImageUrl\", product.rating, product.\"reviewCount\", product.\"isFeatured\", product.\"isTrending\", product.\"createdAt\" FROM product WHERE product.\"isActive\" = true AND product.\"basePrice\" IS NOT NULL AND (product.\"createdAt\", product.id) < (now(), '00000000-0000-0000-0000-000000000000') ORDER BY product.\"createdAt\" DESC, product.id DESC LIMIT 25;" 2>&1 | grep -iE 'Index Scan|Seq Scan'</automated>
  </verify>
  <acceptance_criteria>
    - CLI: the EXPLAIN ANALYZE output contains `Index Scan` (Backward) referencing the composite index `IDX_ad6840fea7aa63e7fad036ae6a` and does NOT contain `Seq Scan on product` (on seeded data).
    - Behavior: the active row count is confirmed >= 200 before the EXPLAIN (so the planner decision is meaningful, not a tiny-table Seq Scan — Pitfall 1).
    - Behavior: the backend is running on :3002 and `curl -s http://localhost:3002/products | head -c 200` returns a JSON object with an `items` array of real products.
    - SUMMARY: the exact EXPLAIN plan line(s) are recorded.
  </acceptance_criteria>
  <done>The seeded keyset query uses the composite (isActive, createdAt, id) index (Index Scan Backward, not Seq Scan), the backend serves real catalog JSON on :3002, and the plan output is recorded — SC4 (index) satisfied.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify the landing page loads/scrolls with no duplicate page-1 request (SC2)</name>
  <files>(no source files — live browser/Network verification; evidence recorded to the SUMMARY)</files>
  <what-built>
    Plan 07-01 flipped the seam to the real source and seeded the DB; Task 1 brought up the backend on :3002 with a verified index. The frontend (:3001) now renders the real catalog grid and feed rails through the createServerFn proxy, with SSR page-1 prefetch under the `['catalog']` key so hydration satisfies the first page without a refetch.
  </what-built>
  <action>
    Claude automates first: confirm the backend from Task 1 is still running on :3002, then start the frontend (`cd shop-front && bun --bun run dev`, port 3001) in the background. Tail the backend log so server-side `GET /products` requests are observable. Then PAUSE for the human to perform the browser Network inspection described in how-to-verify (Claude cannot read browser DevTools). Claude does NOT modify any source to make this pass; if a duplicate page-1 is reported, the documented fix (a `staleTime` on catalogInfiniteQueryOptions) is recorded as a follow-up, not silently applied here.
  </action>
  <how-to-verify>
    Claude automates first: ensure the backend is running (Task 1), then start the frontend (`cd shop-front && bun --bun run dev`, port 3001). Then the human verifies in a browser:
    1. Open http://localhost:3001 with DevTools -> Network open and "Disable cache" checked. Hard-reload (Ctrl/Cmd+Shift+R).
    2. Confirm the grid shows REAL product names/images from the DB (not the mock faker names) and the Featured/Trending/Categories rails are populated.
    3. In the backend terminal/log (or Network tab on the Start server requests), confirm exactly ONE no-cursor `GET /products` (page 1) fires per hard load — NOT two. Page 1 should come from SSR hydration; the browser must not issue its own page-1 fetch on first paint (RESEARCH Pitfall 2). If a second page-1 (background revalidation) fires, note it — a `staleTime` on catalogInfiniteQueryOptions is the documented cosmetic fix, but first confirm whether it actually fires.
    4. Scroll to the bottom of the grid. Confirm a `GET /products?cursor=...` fires exactly ONCE per boundary (not repeatedly), new real products append, and scrolling continues until the end-of-list state appears (no infinite spinner).
    5. Confirm the page renders immediately on load (no long blank flash) and the rails do not entangle the grid's paging.
  </how-to-verify>
  <verify>
    <human-check>Hard reload shows real DB data; exactly one no-cursor GET /products per load (no duplicate page-1); cursor pages fire once per scroll boundary; end-of-list reached cleanly.</human-check>
  </verify>
  <acceptance_criteria>
    - Behavior: the grid and all three rails render real DB data (real product names, not faker-generated mock names).
    - Behavior: on hard reload, exactly one no-cursor `GET /products` is observed server-side; zero browser-issued page-1 fetch on first paint (SC2, Pitfall 2).
    - Behavior: each scroll boundary triggers exactly one cursored `GET /products?cursor=...`; deep scroll reaches the end-of-list state with no infinite spinner.
    - SUMMARY: the page-1 request count per hard load is recorded, plus whether a staleTime mitigation was needed.
  </acceptance_criteria>
  <resume-signal>Type "approved" if the page loads on real data with no duplicate page-1 and once-per-boundary paging, or describe the observed issue (e.g. "two page-1 requests fired").</resume-signal>
  <done>Human confirms the landing page renders real DB data, exactly one no-cursor GET /products fires per hard load (no duplicate page-1), cursor pages fire once per scroll boundary to the end-of-list, and the observation is recorded — SC2 satisfied.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Lighthouse CLS <= 0.1 and confirm the live Zod drift gate passes (SC4)</name>
  <files>(no source files — Lighthouse/web-vitals + curl verification; evidence recorded to the SUMMARY)</files>
  <what-built>
    The real source ends every fetcher with the frozen-schema `.parse()` (the SC4 drift gate). With the stack live (Tasks 1-2), the egress parse runs against REAL backend responses; layout-shift behavior (Phase 5 skeletons + reserved-dimension images) is now exercised on real images.
  </what-built>
  <action>
    Claude automates first: with both servers running, run Lighthouse headless against http://localhost:3001 and capture `audits["cumulative-layout-shift"].numericValue`; if headless Lighthouse is unavailable in this environment, fall back to the installed web-vitals `onCLS` reading (temporary console log from a client entry, read after interaction, then revert with no committed change). Also `curl -s http://localhost:3002/products` and inspect `items[0]` keys. Then PAUSE for the human to confirm the CLS number and the absence of any contract-drift parse error in the frontend console during load+scroll. Claude does NOT alter UI source to improve CLS within this verification task; a CLS regression is recorded as a finding/follow-up.
  </action>
  <how-to-verify>
    Claude automates first: with both servers running, run Lighthouse against the landing page and capture CLS. Prefer `npx lighthouse http://localhost:3001 --only-categories=performance --output=json --quiet --chrome-flags="--headless"` and read `audits["cumulative-layout-shift"].numericValue`. If Lighthouse cannot run headless in this environment, fall back to the web-vitals `onCLS` console reading (web-vitals@5.1.0 is already installed) — temporarily log `onCLS((m) => console.log('CLS', m.value))` from a client entry, read the value after interacting, then revert the temporary log (no committed source change).
    Confirm the live Zod drift gate: with the real backend serving seeded data, the grid/rails rendering without a thrown parse error IS the live drift check (every fetcher `.parse()`s the response). Additionally, the human (or Claude via curl) confirms `curl -s http://localhost:3002/products` returns an object whose `items[0]` has exactly the 9 contract fields (id, name, slug, price, primaryImageUrl, rating, reviewCount, isFeatured, isTrending) and no extra internal columns (no createdAt/basePrice leak).
    The human then confirms the CLS number and that no contract-drift parse error appeared in the frontend console during normal scrolling.
  </how-to-verify>
  <verify>
    <human-check>Lighthouse (or web-vitals onCLS) reports CLS <= 0.1 on http://localhost:3001; the frontend console shows no Zod parse/contract-drift error during load+scroll; live /products items carry exactly the 9 card fields.</human-check>
  </verify>
  <acceptance_criteria>
    - Behavior/CLI: Lighthouse `cumulative-layout-shift` numericValue <= 0.1 (or web-vitals `onCLS` value <= 0.1) on the landing page.
    - Behavior: no Zod/contract-drift error thrown in the frontend during load + full scroll (the live egress `.parse()` passes — SC4 drift gate).
    - CLI: `curl -s http://localhost:3002/products` returns `items` whose first element has exactly the 9 card keys and no leaked internal column (createdAt/basePrice absent).
    - SUMMARY: the measured CLS value and the CLS tool used are recorded; the live drift result is recorded.
  </acceptance_criteria>
  <resume-signal>Type "approved" if CLS <= 0.1 and no contract-drift error occurred, or report the CLS value / the parse error observed.</resume-signal>
  <done>Human confirms CLS <= 0.1, no contract-drift parse error during load+scroll, and live /products items carry exactly the 9 card fields; values recorded — SC4 (CLS + drift) satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Start server | Same-origin only (CSP `connect-src 'self'`); verification confirms no direct :3002 browser call |
| Start server -> NestJS API | createServerFn proxy forwards the cookie; verification observes server-side request counts |
| NestJS API -> Postgres | Parameterized keyset query; EXPLAIN ANALYZE confirms index use on seeded data |
| Real response -> UI | Live Zod egress `.parse()` is the runtime drift gate exercised during verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Information disclosure | Live `GET /products` response shape | mitigate | Verification asserts items carry exactly the 9 card fields with no leaked internal column (createdAt/basePrice); the egress `.parse()` enforces this at runtime |
| T-07-07 | Denial of service | Unbounded scroll / page size against the real endpoint | accept | Backend clamps page size to <= 48 (CAT-03, Phase 6); the grid caps retained pages at maxPages 6; verification observes once-per-boundary paging, not runaway requests |
| T-07-08 | Tampering | EXPLAIN run against the live DB | accept | Read-only EXPLAIN ANALYZE on a dev DB; `SET enable_seqscan = off` (if used) is session-local and does not mutate data or persist |
| T-07-SC | Tampering | npm/bun installs (supply chain) | mitigate | This plan installs NO runtime packages; Lighthouse is run via `npx` (mature Google package, RESEARCH-approved) and is not pinned. If pinned as a devDependency, a blocking-human legitimacy checkpoint is required first |
</threat_model>

<verification>
Phase-level E2E gates (the Phase 7 acceptance set):
- SC2: hard reload shows real data; exactly one no-cursor `GET /products` per load (no duplicate page-1); cursor pages fire once per scroll boundary; end-of-list reached.
- SC4 (index): EXPLAIN ANALYZE shows `Index Scan` (Backward) on the composite `(isActive, createdAt, id)` index on seeded data, not a Seq Scan.
- SC4 (CLS): Lighthouse / web-vitals CLS <= 0.1 on the landing page.
- SC4 (drift): live `.parse()` passes during load+scroll; `/products` items carry exactly the 9 contract fields.
- SC1 (carried from 07-01): full Vitest suite green after the swap, no UI component changed.

All four gates recorded with evidence in 07-02-SUMMARY.md before `/gsd-verify-work`.
</verification>

<success_criteria>
- The landing page loads, scrolls, and paginates on the real `GET /products` with real DB data and NO duplicate page-1 request on hard load (SC2).
- EXPLAIN ANALYZE confirms the composite keyset index is used on seeded data (SC4).
- Lighthouse (or web-vitals onCLS) reports CLS <= 0.1 (SC4).
- The live Zod egress `.parse()` catches no contract drift; `/products` items carry exactly the 9 card fields (SC4).
- All gates recorded in 07-02-SUMMARY.md.
</success_criteria>

<output>
Create `.planning/phases/07-mock-to-real-swap-polish/07-02-SUMMARY.md` when done. Record evidence for every gate: the EXPLAIN ANALYZE plan line(s); the observed page-1 request count per hard load (and whether a staleTime mitigation was applied); the measured CLS value and the tool used; the live Zod drift result; and confirmation that the grid/rails rendered real DB data. Note any deviations (e.g. Lighthouse headless unavailable -> web-vitals fallback).
</output>
