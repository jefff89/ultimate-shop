---
phase: 02-mock-api-layer
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - shop-front/src/data/catalog.source.mock.test.ts
autonomous: true
requirements: [MOCK-01, MOCK-02, MOCK-03]
user_setup: []

must_haves:
  truths:
    - "A page boundary that falls inside a same-createdAt cluster does not skip or duplicate any card — the id DESC tiebreaker is honored"
    - "A tampered or garbage cursor causes InvalidCursorError to propagate from fetchCatalogPage (it does not silently return an empty page)"
    - "Re-evaluating the seeded dataset yields an identical sorted array — cursors stay valid across reloads"
    - "The dataset is varied (has images, prices, ratings, flags) and contains at least one createdAt shared by >=2 products"
    - "UI consumers import fetchCatalogPage from the seam (data/catalog.ts), confirmed by an import-the-seam test"
  artifacts:
    - path: "shop-front/src/data/catalog.source.mock.test.ts"
      provides: "Tiebreaker, tamper-rejection, determinism, variety, and seam-re-export tests (extends Plan 01's suite)"
  key_links:
    - from: "shop-front/src/data/catalog.source.mock.test.ts"
      to: "shop-front/src/data/catalog.ts"
      via: "import fetchCatalogPage from the seam to prove the public entry point"
      pattern: "from './catalog'"
    - from: "shop-front/src/data/catalog.source.mock.test.ts"
      to: "@shared/cursor"
      via: "encodeCursor to build a boundary cursor for the tiebreaker test"
      pattern: "from '@shared/cursor'"
---

<objective>
Harden the mock-API slice with the defense-in-depth and correctness-edge tests that prove the keyset semantics survive the cases a naive implementation silently breaks: a page boundary landing inside a duplicate-timestamp cluster (the `id` tiebreaker), a tampered cursor (security input-validation), dataset variety + timestamp-collision existence (so the tiebreaker path is real, not dead code), determinism across reload, and the seam being the public import surface.

This plan extends the Plan 01 test suite in the same file. It covers the MOCK-01 input-validation edge (tampered cursor), MOCK-02 variety/collision assertions, and MOCK-03 seam + tiebreaker correctness — the cases that make the Plan 01 happy-path slice trustworthy.

Purpose: Prove the mock will not hand the UI (and later the real-backend swap) a skipped or duplicated card at a page boundary with equal timestamps, and that a crafted cursor is rejected loudly rather than swallowed.

Output: Additional `describe`/`it` blocks in `shop-front/src/data/catalog.source.mock.test.ts`.
</objective>

## Phase Goal

**As a** frontend developer relying on the mock seam, **I want to** know the keyset pagination is correct at the hard cases (duplicate timestamps, tampered cursors) and deterministic across reloads, **so that** the infinite grid I build on top never skips or repeats a product and the eventual mock-to-real swap inherits identical, proven semantics.

## Artifacts this phase produces

This plan creates NO new production symbols — it only adds tests to the existing `shop-front/src/data/catalog.source.mock.test.ts` (created in Plan 01). It exercises symbols produced in Plan 01 (`fetchCatalogPage`, `PRODUCTS`-derived behavior, `toCard` output, `MAX_PAGE_SIZE` clamp behavior) and consumes the Phase 1 `@shared/cursor` exports (`encodeCursor`, `InvalidCursorError`) and `@shared/catalog.contract` schema verbatim.

<execution_context>
@/home/jeff/Documents/start-nest-shop/.claude/get-shit-done/workflows/execute-plan.md
@/home/jeff/Documents/start-nest-shop/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-mock-api-layer/02-RESEARCH.md
@.planning/phases/02-mock-api-layer/02-PATTERNS.md
@.planning/phases/02-mock-api-layer/02-01-PLAN.md
@shared/cursor.ts
@shared/catalog.contract.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add tiebreaker, tamper-rejection, determinism, variety, and seam-re-export tests</name>
  <files>shop-front/src/data/catalog.source.mock.test.ts</files>
  <read_first>
    - shop-front/src/data/catalog.source.mock.test.ts (Plan 01's existing suite — extend it, do not duplicate the four existing tests; reuse its traversal helper if one was extracted)
    - shop-front/src/data/catalog.source.mock.ts (Plan 01's source — confirm exported `fetchCatalogPage` signature and the internal `createdAt`-day quantization that produces timestamp clusters)
    - shop-front/src/data/catalog.ts (the seam — the public import target for the seam test)
    - shared/cursor.ts (`encodeCursor`, `InvalidCursorError`; decode throws on tamper — the behavior the tamper test asserts propagates)
    - .planning/phases/02-mock-api-layer/02-PATTERNS.md §"catalog.source.mock.test.ts" (the tamper-rejection mirror: `await expect(fetchCatalogPage({ cursor: '!!!garbage!!!' })).rejects.toThrow()`; the vitest-not-bun:test import note)
    - .planning/phases/02-mock-api-layer/02-RESEARCH.md §"Validation Architecture" Req->Test map (the exact test-name conventions and the tiebreaker/determinism/variety requirements)
  </read_first>
  <behavior>
    Add these tests (import `{ describe, expect, it } from 'vitest'`; import `fetchCatalogPage` from './catalog' (the SEAM) for the seam test and from './catalog.source.mock' where exercising source internals; import `{ encodeCursor } from '@shared/cursor'`):
    - Test "dataset variety and timestamp collisions": collect the full dataset (via a full traversal, reusing Plan 01's walk). Assert it contains at least one product with a non-null primaryImageUrl, at least one with a non-null rating, at least one with isFeatured true and at least one with isTrending true, and that the page count to exhaust the dataset at DEFAULT_PAGE_SIZE is several pages (>= 5). Assert at least one createdAt value is shared by >= 2 products (group the internal sort key — derive it by detecting two adjacent traversal items whose ordering only differs by id, or expose a test-only accessor; if no internal createdAt accessor exists, assert via the cursor: decode is not available client-side, so prove collisions by asserting >= 2 cards share the boundary semantics — prefer adding a tiny test-only named export of the sorted rows' createdAt list from the source if needed for a clean assertion).
    - Test "tiebreaker across equal timestamps": find a page boundary that lands inside a same-createdAt cluster (choose a small limit so a boundary falls mid-cluster), then continue the traversal across that boundary; assert no id appears twice and no id from the cluster is skipped (the union of the two pages' cluster members equals the full cluster). This proves id DESC ordering is honored across the boundary.
    - Test "deterministic across reload": call fetchCatalogPage for the first page twice (or re-import the module via Vitest's resetModules / dynamic import) and assert the returned items (ids in order) are identical — the seeded dataset is stable, so the same cursors resolve to the same rows.
    - Test "rejects tampered cursor": `await expect(fetchCatalogPage({ cursor: '!!!garbage!!!' })).rejects.toThrow()` and also assert a structurally-valid-base64-but-wrong-payload cursor rejects (e.g. `encodeCursor` of a tuple is fine, but a hand-mangled string rejects). Confirm the rejection is the propagated InvalidCursorError, not a returned empty page.
    - Test "seam re-exports fetchCatalogPage": import `{ fetchCatalogPage } from './catalog'` and assert it is a function and returns a contract-valid first page — proving UI consumers can import only from the seam.
  </behavior>
  <action>
    Extend the existing Plan 01 test file with the five tests in the behavior block. Reuse Plan 01's full-traversal walk helper (collect items across pages until hasMore is false, with a hard iteration cap) rather than re-writing it. For the tiebreaker test, pick a `limit` (e.g. 2 or 3) small enough that the dataset's day-quantized clusters guarantee a boundary inside a cluster; if a clean assertion on shared `createdAt` requires it, coordinate with the Plan 01 source to add a single test-only named export (e.g. `__sortedCreatedAtForTest: string[]`) rather than reaching into private state — keep the public seam signature untouched. For the tamper test, use the async `rejects.toThrow()` form (fetchCatalogPage is async) mirroring `cursor.test.ts`'s sync tamper assertions; do NOT wrap calls in try/catch that would mask the throw. For determinism, prefer comparing two same-input calls' id sequences; if testing true re-evaluation, use Vitest `vi.resetModules()` + dynamic `import()` and compare. Apply Prettier style (no semicolons, single quotes, trailing commas).

    If a test-only export is added to catalog.source.mock.ts to make the collision/tiebreaker assertion clean, name it with a `__...ForTest` prefix and document it as test-only in a one-line comment; it must NOT be re-exported from the seam (catalog.ts).
  </action>
  <verify>
    <automated>cd shop-front && bun --bun run test -- src/data/catalog.source.mock.test.ts</automated>
  </verify>
  <done>
    - `cd shop-front && bun --bun run test -- src/data/catalog.source.mock.test.ts` exits 0 with all Plan 01 + Plan 02 tests passing
    - The tiebreaker test proves a boundary inside a same-createdAt cluster neither skips nor duplicates a card
    - The tamper test proves InvalidCursorError propagates (the call rejects; no empty page returned)
    - The variety test proves images/ratings/flags exist and >= 2 products share a createdAt
    - The seam test imports fetchCatalogPage from './catalog' and gets a contract-valid page
    - Any test-only export added to the source is NOT re-exported from catalog.ts
  </done>
  <acceptance_criteria>
    - catalog.source.mock.test.ts imports from 'vitest' (NOT 'bun:test')
    - The file contains a test importing `fetchCatalogPage` from './catalog' (grep: `from './catalog'`)
    - The file contains `rejects.toThrow` for the tamper case
    - The file asserts at least one createdAt is shared by >= 2 products (timestamp collision exists)
    - The tiebreaker test crosses a page boundary inside a cluster and asserts no skip/dupe
    - `cd shop-front && bun --bun run test -- src/data/catalog.source.mock.test.ts` exits 0
    - `cd shop-front && bun --bun run test` (full suite) exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → fetchCatalogPage(cursor) | The `cursor` string is the only structured untrusted input; this plan's tests assert the tamper-rejection behavior at that boundary. |

This plan adds no production code — it is the verification layer for the boundary already established in Plan 01. The threat surface is identical and narrow (in-memory mock, no auth/DB/network).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `fetchCatalogPage` cursor arg | mitigate | The "rejects tampered cursor" test in this plan proves `InvalidCursorError` propagates from the Phase 1 `decodeCursor` rather than being swallowed into an empty page — defense-in-depth verification of the boundary Plan 01 wired. |
| T-02-02 | Information Disclosure | card payload | mitigate | The variety/contract tests confirm `items` carry only the 9 contract fields (validated by `CatalogProductCardPageSchema.parse` in the source); no internal `createdAt`/sort key leaks onto a card. |

Spoofing/Repudiation/DoS/Elevation are not applicable to an unauthenticated in-memory test fixture and are deliberately omitted, not invented.
</threat_model>

<verification>
- `cd shop-front && bun --bun run test -- src/data/catalog.source.mock.test.ts` exits 0 (Plan 01 + Plan 02 tests)
- `cd shop-front && bun --bun run test` exits 0 (full Vitest suite — phase gate)
- `grep -q "from './catalog'" shop-front/src/data/catalog.source.mock.test.ts` (seam import proven)
- `grep -q "rejects.toThrow" shop-front/src/data/catalog.source.mock.test.ts` (tamper rejection asserted)
</verification>

<success_criteria>
- A page boundary inside a same-`createdAt` cluster does not skip or duplicate a card — the `id` DESC tiebreaker is honored (MOCK-03 criterion 3)
- A tampered/garbage cursor causes `InvalidCursorError` to propagate, not a silent empty page (MOCK-01 input validation)
- The seeded dataset is deterministic across reloads so cursors remain valid (MOCK-03)
- The dataset is varied and contains >= 2 products sharing a `createdAt`, making the tiebreaker path real (MOCK-02)
- UI consumers can import `fetchCatalogPage` exclusively from the seam `data/catalog.ts` (MOCK-03)
</success_criteria>

<output>
Create `.planning/phases/02-mock-api-layer/02-02-SUMMARY.md` when done.
</output>
