---
phase: 01-schema-shared-contract
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - shop-front/tsconfig.json
  - shop-front/vite.config.ts
files_modified_note: "No file overlap with Plan 01-02 (backend-only) — runs in parallel in wave 2."
autonomous: true
requirements: [CONT-01, CONT-02]
must_haves:
  truths:
    - "shop-front resolves @shared/* in BOTH tsconfig.json and vite.config.ts (mirroring the existing @/* wiring)"
    - "A frontend module can import CatalogProductCardSchema / catalogPage from @shared/catalog.contract and the project typechecks"
    - "bun --bun run build (shop-front) succeeds with the @shared alias in place"
  artifacts:
    - path: "shop-front/tsconfig.json"
      provides: "@shared/* path alias"
      contains: "@shared/*"
    - path: "shop-front/vite.config.ts"
      provides: "@shared resolve alias (Vite-native, independent of tsconfig)"
      contains: "@shared"
  key_links:
    - from: "shop-front/vite.config.ts"
      to: "shared/"
      via: "fileURLToPath(new URL('../shared', import.meta.url)) alias"
      pattern: "@shared"
    - from: "shop-front/tsconfig.json"
      to: "shared/catalog.contract.ts"
      via: "@shared/* path"
      pattern: "@shared/\\*"
---

<objective>
Wire `@shared/*` resolution into the frontend in BOTH `shop-front/tsconfig.json` AND `shop-front/vite.config.ts` (D-02 — Vite needs the alias independently of tsc), mirroring the existing `@/*` convention, and prove the frontend can import the frozen contract from `@shared/catalog.contract` and still build.

Purpose: Completes the contract-resolution seam for the frontend so the Phase 2 mock and the Phase 7 real client can `import` the shared `CatalogPage<T>` / `CatalogProductCard` shapes with no further wiring. This is the frontend half of ROADMAP criterion 4 (contract importable by both sides). Verifies the alias against installed TanStack/Vite behavior rather than training data, per CLAUDE.md.

Output: `@shared/*` declared in both frontend configs + a build/typecheck proof. Supports CONT-01 and CONT-02 (frontend-resolvable).
</objective>

<execution_context>
@/home/jeff/Documents/start-nest-shop/.claude/get-shit-done/workflows/execute-plan.md
@/home/jeff/Documents/start-nest-shop/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-schema-shared-contract/01-CONTEXT.md
@.planning/phases/01-schema-shared-contract/01-PATTERNS.md
@CLAUDE.md
</context>

<artifacts_consumed>
From Plan 01-01 (must exist before this plan runs):
- `shared/catalog.contract.ts` — `CatalogProductCardSchema`, `catalogPage`, `CatalogProductCardPageSchema`, types. The import-resolution proof targets this file.
</artifacts_consumed>

<tasks>

<task type="auto">
  <name>Task 1: Add @shared/* alias to shop-front tsconfig.json and vite.config.ts</name>
  <files>shop-front/tsconfig.json, shop-front/vite.config.ts</files>
  <read_first>
    - shop-front/tsconfig.json (lines 24-27) — existing `baseUrl: "."` + `paths: { "@/*": ["./src/*"] }`; add a sibling `@shared/*` entry, keep `@/*` intact.
    - shop-front/vite.config.ts (lines 6, 38-43) — `fileURLToPath, URL` already imported (line 6); existing `resolve.alias` with `'@': fileURLToPath(new URL('./src', import.meta.url))`; the `viteTsConfigPaths({ projects: ['./tsconfig.json'] })` plugin (lines 52-54). Add the `@shared` alias following the exact `fileURLToPath(new URL(...))` idiom — no new import needed.
    - .planning/phases/01-schema-shared-contract/01-PATTERNS.md "shop-front/tsconfig.json" + "vite.config.ts" sections — D-02 requires the alias in BOTH files (vite-tsconfig-paths feeds tsconfig paths to Vite, but D-02 wants an explicit vite alias too for robustness).
    - CLAUDE.md — frontend Prettier style (no semicolons, single quotes, trailing commas); `routeTree.gen.ts` is generated (do not touch); TanStack Start is RC/nightly — verify alias resolution actually works against the installed build rather than assuming.
  </read_first>
  <action>
    In `shop-front/tsconfig.json`, add `"@shared/*": ["../shared/*"]` as a sibling of the existing `"@/*": ["./src/*"]` under `compilerOptions.paths` (relative to baseUrl `.` = shop-front/). Keep `@/*` unchanged. In `shop-front/vite.config.ts`, add `'@shared': fileURLToPath(new URL('../shared', import.meta.url)),` as a sibling alias under `resolve.alias` next to the existing `'@'` entry — reuse the already-imported `fileURLToPath`/`URL` (no new import), match Prettier style (single quotes, trailing comma). Leave the `viteTsConfigPaths` plugin and all security-header/Nitro config untouched.
  </action>
  <verify>
    <automated>cd /home/jeff/Documents/start-nest-shop/shop-front && grep -Eq '"@shared/\*"\s*:\s*\[\s*"\.\./shared/\*"\s*\]' tsconfig.json && grep -Eq "'@shared'\s*:\s*fileURLToPath\(new URL\('\.\./shared'" vite.config.ts && echo "alias OK both files"</automated>
  </verify>
  <acceptance_criteria>
    - `shop-front/tsconfig.json` `compilerOptions.paths` contains `"@shared/*": ["../shared/*"]` AND retains `"@/*": ["./src/*"]`.
    - `shop-front/vite.config.ts` `resolve.alias` contains `'@shared': fileURLToPath(new URL('../shared', import.meta.url))` AND retains the `'@'` alias.
    - No new imports added to vite.config.ts; Nitro/CSP/plugin config unchanged.
    - Verify command prints `alias OK both files`.
  </acceptance_criteria>
  <done>The frontend declares `@shared/*` in both tsconfig and vite.config, mirroring `@/*`, with the existing aliases intact.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the frontend resolves @shared/catalog.contract (typecheck + build)</name>
  <files>shop-front/tsconfig.json</files>
  <read_first>
    - shop-front/package.json scripts — `build` (`bun --bun run build`), `check`/`lint` (TanStack eslint-config + tsc). Identify the command that performs a real type-resolution pass over an `@shared` import.
    - shop-front/src/data/signin.ts — an existing module showing how a `data/` file imports and uses a zod schema; the future `data/catalog.ts` will import from `@shared/catalog.contract` the same way (context only — do NOT create data/catalog.ts this phase).
    - shared/catalog.contract.ts (from Plan 01-01) — the import target; confirm the exported symbol names before writing the proof import.
    - CLAUDE.md — TanStack Start RC: verify the alias resolves through the actual Vite/Nitro build, not assumed; `routeTree.gen.ts` is generated.
  </read_first>
  <action>
    Prove `@shared` resolves end-to-end in the frontend toolchain WITHOUT shipping a stray runtime module. Preferred: add a type-only check — a temporary `shop-front/src/__shared-resolution-check.ts` (or a typecheck-only assertion) that does `import type { CatalogProductCard } from '@shared/catalog.contract'` and `import { CatalogProductCardSchema } from '@shared/catalog.contract'`, references both so the import is not elided, then run the frontend typecheck/build and CONFIRM the alias resolves. After the build passes, REMOVE the temporary check file so no scaffold ships (or, if you prefer a durable proof, leave a minimal `shop-front/src/__shared-resolution-check.ts` that is excluded from the route tree and document it). If `bun --bun run build` requires a running backend or DB, fall back to the frontend typecheck command (`bun --bun run check` or the tsc invocation) that still resolves `@shared`. Do not create `data/catalog.ts` (Phase 2 owns it). Do not edit `routeTree.gen.ts`.
  </action>
  <verify>
    <automated>cd /home/jeff/Documents/start-nest-shop/shop-front && printf "import { CatalogProductCardSchema } from '@shared/catalog.contract'\nimport type { CatalogProductCard } from '@shared/catalog.contract'\nconst _c: CatalogProductCard | null = null\nvoid CatalogProductCardSchema\nvoid _c\n" > src/__shared_resolution_check.ts && (bun --bun run build || bun --bun run check) && rm -f src/__shared_resolution_check.ts && echo "shared resolves in frontend"</automated>
  </verify>
  <acceptance_criteria>
    - A frontend module importing `CatalogProductCardSchema` and `CatalogProductCard` from `@shared/catalog.contract` resolves (no "cannot find module @shared/..." error) through the actual frontend toolchain.
    - The frontend build OR typecheck command exits 0 with the alias in place.
    - No stray scaffold remains committed unless explicitly documented (the temp resolution-check file is removed after passing).
    - `routeTree.gen.ts` is unmodified.
  </acceptance_criteria>
  <done>The frontend resolves and typechecks an import from `@shared/catalog.contract`, confirming the alias works in the real Vite/TanStack toolchain — the contract is importable by the frontend mock and real client.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build config → bundle | Alias misconfiguration could silently resolve `@shared` to the wrong path or fail to resolve, shipping a broken or stale contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Tampering | `@shared` alias points to wrong dir | mitigate | Both tsconfig and vite alias use the explicit `../shared` path mirroring the proven `@/*`→`./src` idiom, and the build/typecheck proof asserts a real import from `@shared/catalog.contract` resolves — a wrong path fails the verify. |
| T-01-09 | Information disclosure | contract used on the client | accept | The contract is a public read-only catalog shape (no secrets, no auth fields); shipping it to the browser bundle exposes nothing sensitive. The card schema is intentionally lean (Plan 01-01 freezes 9 non-internal fields). |
| T-01-SC | Tampering | npm installs | n/a here | No package install in this plan — only build-config edits; supply-chain gate for the only new dep (zod) lives in Plan 01-02. |

No high-severity threats; alias-resolution risk is caught by the build/typecheck proof.
</threat_model>

<verification>
- `@shared/*` present in shop-front/tsconfig.json (paths) AND shop-front/vite.config.ts (resolve.alias); `@/*` intact.
- A frontend import from `@shared/catalog.contract` resolves and the frontend build/typecheck passes.
- No `data/catalog.ts` created; `routeTree.gen.ts` untouched.
</verification>

<success_criteria>
- Frontend half of ROADMAP criterion 4: the single shared contract is importable by the frontend (mock + real client) with no further wiring.
- CONT-01, CONT-02 frontend-resolvable.
</success_criteria>

<output>
Create `.planning/phases/01-schema-shared-contract/01-03-SUMMARY.md` when done. The SUMMARY MUST include an "Artifacts this plan produces" section listing: the new `@shared/*` tsconfig path key, the new `@shared` vite resolve alias, and the verification command/result proving frontend resolution. Note that no runtime module was shipped (resolution-check file removed) unless a durable check file was intentionally retained.
</output>
