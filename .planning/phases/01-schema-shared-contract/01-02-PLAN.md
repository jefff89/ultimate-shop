---
phase: 01-schema-shared-contract
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - shop-back/src/products/product.entity.ts
  - shop-back/tsconfig.json
  - shop-back/package.json
  - shop-back/bun.lock
autonomous: false
requirements: [SCHEMA-01, SCHEMA-02, SCHEMA-03, SCHEMA-04, SCHEMA-05, CONT-01]
user_setup:
  - service: postgres
    why: "synchronize:true applies the new columns + composite index only when the backend boots against a live Postgres; verifying the live schema (not a typecheck) requires the local DB to be running."
    dashboard_config:
      - task: "Ensure local Postgres is running with database start_nest_shop_db reachable per shop-back env (DB_HOST/DB_PORT/DB_USER/DB_PASS/DB_NAME)"
        location: "local machine"
must_haves:
  truths:
    - "Product exposes primaryImageUrl, isFeatured, isTrending, nullable rating, nullable reviewCount"
    - "A composite index on (isActive, createdAt, id) exists on the product table in the live database"
    - "shop-back resolves @shared/* and can import the contract for runtime Zod validation"
    - "bun run build (nest build) exits 0 and the built dist still boots"
    - "zod 4.x is a runtime dependency of shop-back, matching shop-front's 4.2.1 major"
  artifacts:
    - path: "shop-back/src/products/product.entity.ts"
      provides: "5 additive columns + composite keyset index on Product"
      contains: "isFeatured"
    - path: "shop-back/tsconfig.json"
      provides: "@shared/* path alias + shared/ compilation strategy"
      contains: "@shared/*"
    - path: "shop-back/package.json"
      provides: "zod 4.x runtime dependency"
      contains: "zod"
  key_links:
    - from: "shop-back/tsconfig.json"
      to: "shared/catalog.contract.ts"
      via: "@shared/* path alias"
      pattern: "@shared"
    - from: "Product entity"
      to: "product table (live DB)"
      via: "TypeORM synchronize on boot"
      pattern: "@Index\\(\\['isActive', 'createdAt', 'id'\\]\\)"
---

<objective>
Firm up the backend `Product` schema strictly additively (D-07): add the five card/merchandising columns and the composite keyset index, adopt Zod 4.x as a backend runtime dependency alongside the existing class-validator DTO pattern (D-03), and wire `@shared/*` resolution into the backend so it can import the frozen contract — without breaking the `nest build` → `bun dist/main.js` production path. Then prove, against the LIVE database, that synchronize applied the columns and index.

Purpose: ROADMAP criteria 1 (columns applied, app boots), 2 (composite index exists for an index-backed keyset seek), 3 (no N+1 via the lean card projection — the card needs no join), and the backend half of criterion 4 (shared contract importable by the real client). Resolves the documented `nest build`/rootDir landmine.

Output: extended `Product` entity, backend `@shared/*` alias + shared compile strategy, `zod` dependency, and a live-DB verification of the applied schema. Implements SCHEMA-01..05 and the backend side of CONT-01.
</objective>

<execution_context>
@/home/jeff/Documents/start-nest-shop/.claude/get-shit-done/workflows/execute-plan.md
@/home/jeff/Documents/start-nest-shop/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-schema-shared-contract/01-CONTEXT.md
@.planning/phases/01-schema-shared-contract/01-PATTERNS.md
@CLAUDE.md
</context>

<artifacts_consumed>
From Plan 01-01 (must exist before this plan runs):
- `shared/catalog.contract.ts` — `CatalogProductCardSchema`, `CatalogProductCardPageSchema`, `catalogPage`, types. The backend imports these for runtime validation (the import-resolution proof in Task 2 depends on this file existing).
- Frozen decision: contract `price`/`rating` are `z.number()`; the backend (Phase 6) converts TypeORM decimal strings → numbers at its boundary. This plan adds the COLUMNS (which TypeORM will return as decimal strings) but does NOT build the projection mapper — that is Phase 6.
</artifacts_consumed>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Supply-chain gate — verify zod legitimacy before install</name>
  <what-built>Nothing yet — this is the supply-chain legitimacy gate that MUST clear before `bun install zod` runs in Task 1. shop-front already depends on `zod@4.2.1`; this plan adds the matching major to shop-back (D-03).</what-built>
  <how-to-verify>
    Confirm `zod` is the legitimate, well-known schema library before installing into shop-back:
    1. Open https://www.npmjs.com/package/zod — confirm it is the canonical Colin McDonnell / `colinhacks` package, millions of weekly downloads, and that `4.2.1` is a real published version.
    2. Confirm shop-front already pins `zod@4.2.1` (it does — verified in patterns) so this is a version-match, not a new/unknown dependency.
  </how-to-verify>
  <resume-signal>Type "approved" to allow the zod install, or describe concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Extend Product entity (additive columns + composite keyset index) and add zod dependency</name>
  <files>shop-back/src/products/product.entity.ts, shop-back/package.json, shop-back/bun.lock</files>
  <read_first>
    - shop-back/src/products/product.entity.ts — the file being modified; copy the EXISTING decorator styles: nullable text (`description`, line 30-31), boolean default (`isActive`, line 39-40), nullable decimal (`basePrice`, line 33-34), int (`totalStock`, line 36-37), class-level `@Index(['name','slug'])` (line 19). All three keyset columns (isActive, createdAt, id) already exist.
    - shop-back/src/product_variants/product-variant.entity.ts — sibling `@Index` style reference (line 12).
    - .planning/phases/01-schema-shared-contract/01-PATTERNS.md "product.entity.ts" section — exact new-column specs and the strictly-additive D-07 constraint (do NOT touch relations, attributes jsonb, or closure-table Category).
    - shop-back/package.json — the `dependencies` block; shop-front pins `zod@4.2.1`.
    - CLAUDE.md — Bun per-workspace install (`bun install` inside shop-back/, prefer bun.lock); every entity must be in AppModule.entities[] (Product ALREADY is — no change); synchronize:true means no migration files.
  </read_first>
  <action>
    In `shop-back/src/products/product.entity.ts`, add EXACTLY these 5 columns (SCHEMA-01/02/03), matching existing decorator styles and using `nullable` where specified so existing rows are safe under synchronize: `primaryImageUrl` as `@Column({ type: 'text', nullable: true })` (mirror `description`); `isFeatured` as `@Column({ default: false })`; `isTrending` as `@Column({ default: false })`; `rating` as `@Column({ type: 'decimal', precision: 3, scale: 2, nullable: true })` (0.00–5.00); `reviewCount` as `@Column({ type: 'int', nullable: true })`. Add the composite keyset index (SCHEMA-05) as a second class-level decorator stacked under the existing one: `@Index(['isActive', 'createdAt', 'id'])`. Do NOT add `rootDir`, do NOT modify relations/`attributes`/Category (D-07). No new TypeORM imports needed (`Column`, `Index` already imported). Then add `zod` to `shop-back/package.json` dependencies pinned to match the frontend major (`"zod": "4.2.1"` exact, to eliminate skew per D-03) and run `bun install` inside `shop-back/` to update `bun.lock`. Match the existing 2-space JSON formatting.
  </action>
  <verify>
    <automated>cd /home/jeff/Documents/start-nest-shop/shop-back && grep -v '^\s*//' src/products/product.entity.ts | grep -Eq "primaryImageUrl" && grep -v '^\s*//' src/products/product.entity.ts | grep -Eq "isFeatured" && grep -v '^\s*//' src/products/product.entity.ts | grep -Eq "isTrending" && grep -v '^\s*//' src/products/product.entity.ts | grep -Eq "rating" && grep -v '^\s*//' src/products/product.entity.ts | grep -Eq "reviewCount" && grep -Eq "@Index\(\['isActive', 'createdAt', 'id'\]\)" src/products/product.entity.ts && grep -q '"zod"' package.json && test -f node_modules/zod/package.json && echo "entity+zod OK"</automated>
  </verify>
  <acceptance_criteria>
    - `product.entity.ts` declares all 5 columns: `primaryImageUrl` (text nullable), `isFeatured` (bool default false), `isTrending` (bool default false), `rating` (decimal(3,2) nullable), `reviewCount` (int nullable).
    - The class carries `@Index(['isActive', 'createdAt', 'id'])` in addition to the existing `@Index(['name','slug'])`.
    - No relation, `attributes` jsonb, or Category change (D-07 — diff touches only the 5 columns + 1 index decorator).
    - `shop-back/package.json` dependencies include `zod` at `4.2.1` and `node_modules/zod` is installed.
    - Verify command prints `entity+zod OK`.
  </acceptance_criteria>
  <done>Product is extended strictly additively with the 5 card/merchandising columns and the composite keyset index, and zod 4.2.1 is installed in shop-back.</done>
</task>

<task type="auto">
  <name>Task 2: Wire @shared/* into shop-back without breaking nest build / dist boot</name>
  <files>shop-back/tsconfig.json</files>
  <read_first>
    - shop-back/tsconfig.json — current file has NO `paths`, NO `include`, NO explicit `rootDir`; `outDir: ./dist`, `baseUrl: ./`, `module/moduleResolution: nodenext`. Keep `experimentalDecorators`, `emitDecoratorMetadata`, `strictNullChecks`.
    - shop-back/nest-cli.json — `sourceRoot: src`, `deleteOutDir: true`; `nest build` runs tsc and emits to dist; `start:prod` is `bun dist/main.js`.
    - .planning/phases/01-schema-shared-contract/01-PATTERNS.md "shop-back/tsconfig.json" section — the build-tooling landmine: adding out-of-`src` inputs (e.g. `../shared` in `include`) makes tsc infer a higher common rootDir and shifts dist layout, breaking `dist/main.js`. Enumerated approaches (a) include+paths, (b) project reference, (c) build-time copy.
    - shop-front/tsconfig.json (lines 24-27) — the `@/*` paths convention to mirror as `@shared/*`.
    - CLAUDE.md — Bun resolves tsconfig `paths` natively for `bun --watch src/main.ts` (dev works); the compiled dist output is the risk; "verify against installed types where behavior is uncertain."
  </read_first>
  <action>
    Add `"paths": { "@shared/*": ["../shared/*"] }` under `compilerOptions` in `shop-back/tsconfig.json` (mirrors the FE alias, D-02), and choose ONE compilation strategy that keeps `dist/main.js` at its current path AFTER `nest build`. Recommended approach to evaluate first: set an explicit `"rootDir": "./src"` and DO NOT add `../shared` to `include`; instead make the backend import the shared contract in a way that compiles cleanly under `nest build` — verify empirically whether nest build with the `@shared/*` path alias + a TypeScript import resolves and emits without pulling `shared/` into `dist/` and without moving `dist/main.js`. If a raw `@shared` import breaks the emit layout or the dist runtime cannot resolve it, fall back to a documented strategy: either (b) a TS project reference to a `shared/tsconfig.json`, or (c) a build step that compiles/copies `shared/` next to dist and a matching runtime resolution. Whichever you pick, the dist output MUST keep `dist/main.js` as the entry (so `bun dist/main.js` is unchanged) and `bun dist/main.js` MUST boot. Document the chosen strategy and WHY in the SUMMARY. Do not regress decorator/strictNullChecks options.
  </action>
  <verify>
    <automated>cd /home/jeff/Documents/start-nest-shop/shop-back && grep -q '@shared/\*' tsconfig.json && bun run build && test -f dist/main.js && echo "build OK dist/main.js present"</automated>
  </verify>
  <acceptance_criteria>
    - `shop-back/tsconfig.json` declares `"@shared/*": ["../shared/*"]` under `compilerOptions.paths`.
    - `bun run build` (nest build) exits 0.
    - `dist/main.js` exists at the same path after build (entry point unchanged; `start:prod` `bun dist/main.js` still valid).
    - The chosen shared-compilation strategy is documented in the SUMMARY with the rationale and any new files (e.g. shared/tsconfig.json) listed.
    - A backend module can `import` from `@shared/catalog.contract` and tsc resolves it (prove with a temporary import or an existing one that the build compiles).
  </acceptance_criteria>
  <done>The backend resolves `@shared/*`, `nest build` succeeds, and the built `dist/main.js` is intact — the build landmine is resolved and the contract is importable for runtime validation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint [BLOCKING]: Boot backend + verify live DB columns and composite index (synchronize)</name>
  <what-built>The Product entity now declares 5 new columns + the composite keyset index, applied automatically by TypeORM `synchronize:true` on backend boot (no migration files exist — `migration:run` does NOT apply here). This [BLOCKING] task verifies the LIVE database, because a passing typecheck/build is a false positive: types come from the entity file, not the live schema. Requires a running local Postgres (`start_nest_shop_db`); flagged autonomous:false for manual run if the DB is not guaranteed in the executor env.</what-built>
  <how-to-verify>
    1. Ensure local Postgres is running and reachable with the shop-back env (DB_HOST/DB_PORT/DB_USER/DB_PASS/DB_NAME → `start_nest_shop_db`).
    2. Boot the backend so synchronize applies the schema: `cd shop-back && bun run start:dev` (or `bun dist/main.js` after Task 2's build). Wait until it logs a successful start with no schema errors. (ROADMAP criterion 1: the app boots with the new columns applied.)
    3. With the app booted, verify the COLUMNS exist in the live DB — run against `start_nest_shop_db`:
       `psql start_nest_shop_db -c "SELECT column_name FROM information_schema.columns WHERE table_name='product' AND column_name IN ('primaryImageUrl','isFeatured','isTrending','rating','reviewCount') ORDER BY column_name;"`
       Expect all 5 rows returned.
    4. Verify the composite INDEX exists (ROADMAP criterion 2):
       `psql start_nest_shop_db -c "SELECT indexname, indexdef FROM pg_indexes WHERE tablename='product' AND indexdef ILIKE '%isActive%createdAt%id%';"`
       Expect at least one index whose definition covers (isActive, createdAt, id) in that order.
    5. Confirm the app boots without TypeORM synchronize errors in the logs.
  </how-to-verify>
  <resume-signal>Type "approved" once all 5 columns and the composite (isActive, createdAt, id) index are confirmed present in the live DB and the app booted clean; otherwise paste the psql output / boot error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package registry → shop-back | New `zod` dependency pulled via Bun is third-party code entering the backend. |
| entity definition → live Postgres | synchronize:true mutates the live schema from entity decorators on boot. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | `bun install zod` into shop-back | mitigate | Blocking human supply-chain checkpoint precedes the install: confirm `zod` on npmjs.com is the canonical colinhacks package and `4.2.1` matches the version already trusted in shop-front (not a new/unknown dep). |
| T-01-05 | Tampering | synchronize:true schema apply | mitigate | New columns are additive + nullable/defaulted, so synchronize cannot drop or destructively alter existing data; D-07 forbids relation/Category reshaping. Verified against the LIVE DB (not just a typecheck) by the blocking task. |
| T-01-06 | Denial of service | nest build emit-path shift | mitigate | Build strategy explicitly keeps `dist/main.js` intact and the verify step asserts the file exists + the build exits 0, preventing a broken `start:prod`. |
| T-01-07 | Information disclosure | runtime Zod validation on backend (D-03, future) | accept | Validating the response against the shared schema before sending is a hardening control; full enforcement lands in Phase 6's projection — this plan only makes the schema importable. No high-severity exposure introduced here. |

No high-severity threats remain open; supply-chain risk is gated by a blocking human checkpoint.
</threat_model>

<verification>
- Entity carries 5 new columns + composite index; diff is strictly additive (D-07).
- `bun run build` exits 0 and `dist/main.js` is intact.
- LIVE DB shows all 5 columns and the (isActive, createdAt, id) index (psql verification, not typecheck).
- `zod@4.2.1` installed in shop-back; `@shared/*` resolves under nest build.
</verification>

<success_criteria>
- ROADMAP criterion 1: Product exposes the new columns and the app boots with them applied (live-DB verified).
- ROADMAP criterion 2: composite (isActive, createdAt, id) index exists for an index-backed keyset seek.
- ROADMAP criterion 3: lean card projection needs no join (columns are denormalized onto Product) — no N+1 by design.
- Backend half of criterion 4: shared contract importable by the backend.
- SCHEMA-01..05 satisfied; CONT-01 backend-resolvable.
</success_criteria>

<output>
Create `.planning/phases/01-schema-shared-contract/01-02-SUMMARY.md` when done. The SUMMARY MUST include an "Artifacts this plan produces" section listing: the 5 new Product columns (names + types + nullability), the composite index name/definition as it appears in pg_indexes, the new `@shared/*` tsconfig alias key, the chosen shared-compilation strategy (and any new files like shared/tsconfig.json), and the zod version added to shop-back.
</output>
