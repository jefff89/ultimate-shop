---
phase: 05-motion-loading-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shop-front/src/components/catalog/ProductCardSkeleton.tsx
  - shop-front/src/components/catalog/ProductCardSkeleton.test.tsx
  - shop-front/src/components/catalog/ProductGrid.tsx
  - shop-front/src/components/SafeImage.tsx
  - shop-front/src/components/SafeImage.test.tsx
  - shop-front/src/components/catalog/ProductCard.tsx
  - shop-front/src/components/catalog/ProductCard.test.tsx
autonomous: true
requirements: [MOT-01, MOT-02, MOT-04]
must_haves:
  truths:
    - "While the first catalog page loads, the grid shows pixel-identical pulse skeletons (no broken/empty layout)"
    - "Swapping a skeleton for a real card causes zero layout shift (CLS <= 0.1)"
    - "A dead/broken image URL renders a neutral fallback box, never the browser broken-image glyph or a hang"
    - "Hovering a product card lifts it slightly (transform + shadow only), respecting reduced-motion"
    - "Reduced-motion users see no card-lift transform animation"
  artifacts:
    - path: "shop-front/src/components/catalog/ProductCardSkeleton.tsx"
      provides: "Pulse skeleton matching ProductCard box dimensions"
      exports: ["default"]
      min_lines: 15
    - path: "shop-front/src/components/SafeImage.tsx"
      provides: "onError fallback to a neutral muted-icon box, scheme validation retained"
    - path: "shop-front/src/components/catalog/ProductCard.tsx"
      provides: "motion-safe hover lift on the outer article"
  key_links:
    - from: "shop-front/src/components/catalog/ProductGrid.tsx"
      to: "shop-front/src/components/catalog/ProductCardSkeleton.tsx"
      via: "isPending initial-load render maps skeleton placeholders"
      pattern: "ProductCardSkeleton"
    - from: "shop-front/src/components/catalog/ProductCard.tsx"
      to: "shop-front/src/components/SafeImage.tsx"
      via: "card image renders through SafeImage (fallback applies)"
      pattern: "SafeImage"
---

<objective>
Deliver three independent motion/loading polish changes to the landing page: (1) MOT-01 pixel-identical pulse skeletons for the grid's initial load, (2) MOT-02 a graceful onError image fallback in SafeImage, and (3) MOT-04 a restrained motion-safe hover lift on the product card.

Purpose: The page must feel polished while loading and degrade gracefully on broken images, with zero layout shift, carrying forward the existing zinc/pulse design language from Phase 4.
Output: A new `ProductCardSkeleton` component, an `onError`-aware `SafeImage`, a hover-lift `ProductCard`, the grid wired to show skeletons on initial load, and tests for each.
</objective>

<execution_context>
@/home/jeff/Documents/start-nest-shop/.claude/get-shit-done/workflows/execute-plan.md
@/home/jeff/Documents/start-nest-shop/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-motion-loading-polish/05-UI-SPEC.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProductCardSkeleton + grid initial-load skeletons (MOT-01)</name>
  <files>shop-front/src/components/catalog/ProductCardSkeleton.tsx, shop-front/src/components/catalog/ProductCardSkeleton.test.tsx, shop-front/src/components/catalog/ProductGrid.tsx</files>
  <read_first>
    - shop-front/src/components/catalog/ProductCard.tsx (the box this skeleton must match pixel-for-pixel: outer `rounded-xl border border-zinc-200`, `aspect-square` image well `bg-zinc-100`, body `flex flex-1 flex-col gap-1 p-3` with a `text-sm` name line + `text-base` price line)
    - shop-front/src/components/feed/Rail.tsx (canonical pulse skeleton pattern: `animate-pulse rounded-xl bg-zinc-100`, container `role="status"` + `aria-busy="true"` + `sr-only` label)
    - shop-front/src/components/catalog/ProductGrid.tsx (the grid that gains an initial-load branch; reuses its existing `grid grid-cols-2 gap-4 md:grid-cols-3 lg:grid-cols-4` wrapper)
    - shop-front/src/components/catalog/ProductGrid.test.tsx (test conventions: @vitest-environment jsdom, vi.mock '@/data/catalog', QueryClient wrapper with retry:false)
    - .planning/phases/05-motion-loading-polish/05-UI-SPEC.md (Motion & Loading Contract -> Skeletons: locked values)
  </read_first>
  <action>
    Create `ProductCardSkeleton.tsx` as a default-exported presentational component mirroring `ProductCard`'s box exactly so a skeleton->card swap is zero-CLS. Outer element: `flex flex-col overflow-hidden rounded-xl border border-zinc-200 bg-white`. Inside: an `aspect-square bg-zinc-100 animate-pulse` image well, then a body `flex flex-1 flex-col gap-1 p-3` containing two pulse bars — a name bar (`h-4 ... animate-pulse rounded bg-zinc-100`, roughly the `text-sm` line) and a shorter price bar (`h-5 w-1/3 animate-pulse rounded bg-zinc-100`, roughly the `text-base` line). Mark the skeleton's purely-visual bars decorative; the skeleton itself is presentational with no remote data.

    In `ProductGrid.tsx`, add an initial-load branch using the `isPending` (first-page-loading) flag from `useInfiniteQuery` (read its current return values — destructure `isPending` alongside the existing `data, fetchNextPage, hasNextPage, isFetchingNextPage`). When `isPending` is true (page 1 not yet resolved), render a `role="status"` `aria-busy="true"` container wrapping the SAME `grid grid-cols-2 gap-4 md:grid-cols-3 lg:grid-cols-4` layout filled with ~8 `ProductCardSkeleton` placeholders, plus a `<span className="sr-only">Loading products…</span>`. Add `data-testid="grid-skeleton"` to that container. Do NOT alter the existing success/end-of-list/`grid-loading`/sentinel behavior — the skeleton is strictly the initial `isPending` state. No semicolons, single quotes, trailing commas; import via the `@/` alias.

    Create `ProductCardSkeleton.test.tsx` (@vitest-environment jsdom) asserting the skeleton renders pulse blocks with `animate-pulse`/`bg-zinc-100` and the matching outer `rounded-xl border border-zinc-200` box.
  </action>
  <verify>
    <automated>cd /home/jeff/Documents/start-nest-shop/shop-front && node node_modules/vitest/vitest.mjs run src/components/catalog/ProductCardSkeleton.test.tsx src/components/catalog/ProductGrid.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `ProductCardSkeleton.tsx` exists and default-exports a component whose outer box uses `rounded-xl`, `border`, `border-zinc-200` and contains an `aspect-square`, `bg-zinc-100`, `animate-pulse` image well (source assertion).
    - The skeleton body uses `flex`, `flex-col`, `gap-1`, `p-3` (matches ProductCard body so the swap is zero-CLS).
    - `ProductGrid.tsx` renders a `data-testid="grid-skeleton"` container with `role="status"` and `aria-busy="true"` and an `sr-only` label reading `Loading products…` while `isPending` is true (source assertion).
    - The skeleton container reuses the exact grid classes `grid grid-cols-2 gap-4 md:grid-cols-3 lg:grid-cols-4` (source assertion).
    - Existing ProductGrid tests still pass and the new ProductCardSkeleton test passes via `node node_modules/vitest/vitest.mjs run` (NOT `bun --bun run test` — known crash).
    - No `animate-pulse` shimmer/spinner introduced — pulse only (source assertion).
  </acceptance_criteria>
  <done>Grid shows pixel-identical pulse skeletons during initial page-1 load; swap to real cards is zero-CLS; suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SafeImage onError fallback box (MOT-02)</name>
  <files>shop-front/src/components/SafeImage.tsx, shop-front/src/components/SafeImage.test.tsx</files>
  <behavior>
    - Test 1: a scheme-valid `https` src that fires `onError` swaps to a fallback box (no native broken-image `<img>` remains; a decorative `aria-hidden` icon is shown).
    - Test 2: a `javascript:` / `blob:` / `file:` src is still rejected (scheme validation intact) — renders fallback/null, never the unsafe `<img src>`.
    - Test 3: same-origin (`/...`), `https:`, and `data:image/...` srcs still render an `<img>` on the happy path before any error.
    - Test 4: the reserved box dimensions (`width`/`height`/`aspect-square` wrapper) are identical between the loaded and fallback states (CLS invariant).
    - Test 5: the product `alt` is preserved for screen readers in the fallback state; the icon itself is `aria-hidden="true"`.
  </behavior>
  <read_first>
    - shop-front/src/components/SafeImage.tsx (current: `isSafeImageSrc` scheme validation — KEEP it; allows leading `/` same-origin, `data:image/`, `https:` with optional `allowedHosts`; rejects everything else)
    - shop-front/src/components/catalog/ProductCard.tsx (the consumer: passes `src`, `alt`, `loading="lazy"`, `width={400} height={400}`, and an image-well className; the fallback must occupy the same reserved box)
    - .planning/phases/05-motion-loading-polish/05-UI-SPEC.md (Motion & Loading Contract -> Lazy images: the broken-URL fallback contract — neutral `bg-zinc-100` reserved box, centered muted `lucide` icon, no glyph/hang/destructive color)
    - shop-front/src/components/feed/Rail.test.tsx (test conventions: @vitest-environment jsdom, render/screen from @testing-library/react, afterEach cleanup)
  </read_first>
  <action>
    Add client-side `onError` state to `SafeImage` (convert to a function component holding a `useState` error flag, or extract a thin wrapper — keep the default export name `SafeImage` and the existing `SafeImageProps` shape). Keep `isSafeImageSrc` and all scheme rules byte-for-byte: still reject `javascript:`/`blob:`/`file:`, still allow same-origin `/`, `https:` (with optional `allowedHosts`), and `data:image/`.

    On a scheme-valid src: render the `<img>` with `onError` setting the error flag. When the error flag is set (or `resolved` is null because the src failed scheme validation and no `fallback` was supplied), render a neutral fallback in the SAME reserved box: a `flex items-center justify-center bg-zinc-100` container sized to the passed `width`/`height` (or filling the `aspect-square` well via `h-full w-full`) holding a centered `lucide-react` `ImageOff` icon colored `text-muted-foreground`, marked `aria-hidden="true"`. Preserve the `alt` for screen readers (e.g. an `sr-only` label or `role="img" aria-label={alt}` on the fallback container). FORBIDDEN: the native broken-image glyph, `dangerouslySetInnerHTML`, any error/destructive color, any layout shift, any spinner. Import the icon from `lucide-react` (already a dependency). No semicolons, single quotes, trailing commas.

    Write/extend `SafeImage.test.tsx` (@vitest-environment jsdom) covering the five behaviors above — drive `onError` by firing the image's error event and assert the icon/box replaces the `<img>` while the box dimensions are unchanged, and assert the unsafe-scheme rejection still holds.
  </action>
  <verify>
    <automated>cd /home/jeff/Documents/start-nest-shop/shop-front && node node_modules/vitest/vitest.mjs run src/components/SafeImage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `isSafeImageSrc` scheme logic is unchanged: `javascript:`/`blob:`/`file:` rejected; `/`-prefixed same-origin, `https:`, `data:image/` allowed (source + test assertion).
    - Firing `onError` on a scheme-valid image replaces the `<img>` with a `bg-zinc-100` box containing a `lucide-react` icon (`ImageOff` or `Image`) in `text-muted-foreground`, `aria-hidden="true"` (test assertion).
    - Reserved box dimensions (`width`/`height` or `aspect-square`/`h-full w-full`) are identical between loaded and fallback states — no layout shift (source assertion).
    - The product `alt` (product name) is still exposed to assistive tech in the fallback state (test assertion).
    - No `dangerouslySetInnerHTML`, no native broken-image glyph fallthrough, no destructive coloring (source assertion).
    - Suite green via `node node_modules/vitest/vitest.mjs run` (NOT bun).
  </acceptance_criteria>
  <done>Dead/broken image URLs render a neutral muted-icon box in the reserved dimensions; scheme validation intact; suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ProductCard motion-safe hover lift (MOT-04)</name>
  <files>shop-front/src/components/catalog/ProductCard.tsx, shop-front/src/components/catalog/ProductCard.test.tsx</files>
  <behavior>
    - Test 1: the outer `<article>` carries a base `shadow-sm`, a `transition` utility, and `motion-safe:hover:-translate-y-0.5` + `hover:shadow-md` (hover lift via transform + shadow only).
    - Test 2: the existing image `group-hover:scale-105 transition-transform duration-300` is retained on the inner SafeImage (not removed by the change).
    - Test 3: no layout-affecting hover class (width/height/margin/top/`hover:m`/`hover:w`/`hover:h`) is present on the article.
  </behavior>
  <read_first>
    - shop-front/src/components/catalog/ProductCard.tsx (target: the outer `<article className="group flex flex-col overflow-hidden rounded-xl border border-zinc-200 bg-white">` and the inner image `transition-transform duration-300 group-hover:scale-105` — KEEP the image transition)
    - .planning/phases/05-motion-loading-polish/05-UI-SPEC.md (Motion & Loading Contract -> Card hover lift: locked values — `translateY(-2px)` via `-translate-y-0.5`, `shadow-sm -> shadow-md`, ~200ms, transform/opacity/box-shadow only, `motion-safe`-gated)
  </read_first>
  <action>
    On the outer `<article>` in `ProductCard.tsx`, add a base `shadow-sm`, a `transition` (covering transform + box-shadow, ~200ms — e.g. `transition duration-200`), and the hover lift `motion-safe:hover:-translate-y-0.5 hover:shadow-md`. The transform MUST be gated under `motion-safe:` so reduced-motion users get no movement; the shadow change may apply on plain `hover:`. Animate ONLY transform / opacity / box-shadow — NO width/height/margin/top or other layout properties. RETAIN the existing inner image classes `transition-transform duration-300 group-hover:scale-105` exactly (do not remove the `group-hover:scale-105`). No semicolons, single quotes, trailing commas.

    Extend `ProductCard.test.tsx` (create it if absent, @vitest-environment jsdom, render a card via a representative `CatalogProductCard` like the `makeCard` shape in ProductGrid.test.tsx) asserting the three behaviors above by inspecting the article's className and the image's className.
  </action>
  <verify>
    <automated>cd /home/jeff/Documents/start-nest-shop/shop-front && node node_modules/vitest/vitest.mjs run src/components/catalog/ProductCard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The outer `<article>` className contains `shadow-sm`, a `transition` utility, `motion-safe:hover:-translate-y-0.5`, and `hover:shadow-md` (source + test assertion).
    - The inner image still carries `transition-transform duration-300 group-hover:scale-105` (source + test assertion).
    - No layout-affecting hover utility (`hover:w-`, `hover:h-`, `hover:m`, `hover:top`, etc.) appears on the article (source assertion).
    - Transition duration is ~200ms (e.g. `duration-200`) on the article lift (source assertion).
    - Suite green via `node node_modules/vitest/vitest.mjs run` (NOT bun).
  </acceptance_criteria>
  <done>Cards lift one restrained step (transform + shadow) on hover, motion-safe gated, image scale retained; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend/mock data -> rendered card | `primaryImageUrl` and `name` are data-controlled and cross into rendered DOM (images + text) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | `SafeImage` onError fallback (MOT-02) | mitigate | Keep `isSafeImageSrc` scheme validation byte-for-byte: still reject `javascript:`/`blob:`/`file:`; allow only `/` same-origin, `https:` (optional `allowedHosts`), `data:image/`. Fallback must not reintroduce an unvalidated `<img src>`. |
| T-05-02 | Tampering | new loading/fallback/skeleton copy + labels | mitigate | All new copy (`Loading products…`, fallback `alt`) rendered as escaped JSX children / standard attributes only. No `dangerouslySetInnerHTML` anywhere in this plan (carry-forward Phase-4 XSS posture). |
| T-05-03 | Denial of Service | broken/hanging image URL (MOT-02) | mitigate | `onError` switches to a static fallback box — no spinner/hang, no retry loop; reserved dimensions stay fixed so a flood of broken images cannot trigger layout thrash. |
| T-05-SC | Tampering | npm/bun installs | accept | No new packages installed this plan; `lucide-react@^0.544.0` is already an installed dependency. No package-legitimacy gate required. |
</threat_model>

<verification>
- `node node_modules/vitest/vitest.mjs run` (full shop-front suite) stays green — no regressions to the Phase 3/4 suites.
- `bun --bun run check` (lint + format) passes for the modified files (no-semicolons / single-quotes / trailing-commas).
- Visual/CLS confirmation of skeleton->card and image-fallback box invariance is covered by the Phase-level human-verify (deferred to phase verification, not blocking here).
</verification>

<success_criteria>
- MOT-01: grid renders pixel-identical pulse skeletons on initial load (`grid-skeleton`, `role="status"`, `aria-busy`, `sr-only`), zero CLS on swap.
- MOT-02: dead/broken image URL renders a neutral muted-icon fallback box in the reserved dimensions; scheme validation intact.
- MOT-04: card hover lift (`motion-safe:hover:-translate-y-0.5` + `hover:shadow-md`, ~200ms), image `scale-105` retained.
- All new/modified component tests pass via `node node_modules/vitest/vitest.mjs run`.
</success_criteria>

<output>
Create `.planning/phases/05-motion-loading-polish/05-01-SUMMARY.md` when done.
</output>
