---
phase: 05-motion-loading-polish
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - shop-front/src/hooks/useReveal.ts
  - shop-front/src/components/Reveal.tsx
  - shop-front/src/components/Reveal.test.tsx
  - shop-front/src/components/catalog/ProductGrid.tsx
  - shop-front/src/components/feed/FeaturedRail.tsx
  - shop-front/src/components/feed/TrendingRail.tsx
  - shop-front/src/components/feed/CategoriesRail.tsx
autonomous: true
requirements: [MOT-03]
must_haves:
  truths:
    - "With JavaScript disabled (SSR/no-JS), every revealable item renders fully visible (opacity 1, no transform)"
    - "Server HTML and first client render agree — no hydration mismatch"
    - "After hydration, grid cards and rail items fade+rise as they enter the viewport"
    - "Reduced-motion users get no reveal animation — content is in its final state immediately"
    - "Items reveal with a slight per-item stagger within a rail/grid"
  artifacts:
    - path: "shop-front/src/hooks/useReveal.ts"
      provides: "IntersectionObserver-driven, post-mount, motion-safe reveal state hook"
      exports: ["useReveal"]
      min_lines: 20
    - path: "shop-front/src/components/Reveal.tsx"
      provides: "SSR-safe wrapper that renders children visible by default and applies the fade+rise only post-hydration when motion is safe"
      exports: ["default"]
  key_links:
    - from: "shop-front/src/components/Reveal.tsx"
      to: "shop-front/src/hooks/useReveal.ts"
      via: "wrapper consumes the reveal hook"
      pattern: "useReveal"
    - from: "shop-front/src/components/catalog/ProductGrid.tsx"
      to: "shop-front/src/components/Reveal.tsx"
      via: "grid cards wrapped in Reveal with per-item stagger index"
      pattern: "Reveal"
---

<objective>
Implement MOT-03: a minimal, motion-safe reveal-on-scroll (fade + small rise) applied to grid cards and rail items, with a fully-visible SSR/no-JS baseline and NO hydration mismatch. Built as a reusable `useReveal` hook + a `Reveal` wrapper, then applied to the grid and the three rails.

Purpose: Add restrained polish without ever hiding content from no-JS users or reduced-motion users, and without animating layout properties. This is the highest-risk requirement in the phase (TanStack Start RC SSR) — the SSR-safe, no-mismatch baseline is a hard acceptance criterion.
Output: `useReveal` hook, `Reveal` wrapper + test, and the grid + Featured/Trending/Categories rails wrapping their items in `Reveal`.
</objective>

<execution_context>
@/home/jeff/Documents/start-nest-shop/.claude/get-shit-done/workflows/execute-plan.md
@/home/jeff/Documents/start-nest-shop/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-motion-loading-polish/05-UI-SPEC.md
@./CLAUDE.md
@.planning/phases/05-motion-loading-polish/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useReveal hook + SSR-safe Reveal wrapper (MOT-03 core)</name>
  <files>shop-front/src/hooks/useReveal.ts, shop-front/src/components/Reveal.tsx, shop-front/src/components/Reveal.test.tsx</files>
  <behavior>
    - Test 1 (SSR/no-JS baseline): the wrapper's INITIAL render (before any effect runs) renders children with NO `opacity-0` / hidden style and NO transform — fully visible. Assert there is no inline `style={{ opacity: 0 }}` and no `opacity-0` class in the initial markup.
    - Test 2 (post-mount, motion-safe): after mount, when motion is safe and the element has not yet entered the viewport, a `motion-safe`-gated pre-reveal class/data-attribute is applied; once the IntersectionObserver reports intersection, a `revealed` data-attribute/class is set producing the final visible state.
    - Test 3 (reduced-motion): when `prefers-reduced-motion: reduce`, the wrapper applies NO pre-reveal hidden state — content stays in final state, no animation classes added.
    - Test 4 (stagger): a `delay`/index prop produces an incremental per-item transition delay (e.g. a `transitionDelay` style or a delay utility scaled by index).
    - Test 5 (no layout props): the only animated properties referenced are opacity and transform (translateY) — never width/height/margin/top.
  </behavior>
  <read_first>
    - .planning/phases/05-motion-loading-polish/05-UI-SPEC.md (Motion & Loading Contract -> Reveal-on-scroll: LOCKED — opacity 0->1 + translateY(8px->0), ~400ms ease-out, slight per-item stagger, `motion-safe` gated, SSR fully visible, no inline opacity:0 on SSR, no hydration mismatch, no animating layout properties)
    - shop-front/src/components/catalog/ProductGrid.tsx (one consumer: grid card map — `useInfiniteQuery` based IntersectionObserver already used here; reuse the same observer-mocking pattern in tests)
    - shop-front/src/components/catalog/ProductGrid.test.tsx (test conventions: @vitest-environment jsdom, MockIntersectionObserver capturing the callback via `vi.stubGlobal('IntersectionObserver', ...)`, `fireIntersection` helper; reuse this exact pattern)
    - .planning/STATE.md (Blockers: TanStack Start RC SSR is the highest-risk integration — the reveal must be PURE progressive enhancement, never load-bearing for visibility)
  </read_first>
  <action>
    Create `src/hooks/useReveal.ts` exporting `useReveal`. The hook returns the state needed to drive the wrapper: a `ref` to attach to the target element and a `revealed` boolean (and/or a `shouldAnimate` flag). Implementation rules:
    - The hook must start in a NON-hidden state so the very first client render matches the server (both fully visible). Use a mounted/hydrated guard (a `useState(false)` flipped in `useEffect`) so the "pre-reveal hidden" styling is only ever applied AFTER mount — never during SSR or first hydration render.
    - Detect reduced motion via `window.matchMedia('(prefers-reduced-motion: reduce)')`. If reduced motion is preferred (or `matchMedia`/`IntersectionObserver` is unavailable), short-circuit to `revealed = true` and apply no animation.
    - When motion is safe, after mount create an `IntersectionObserver` on the ref; on first intersection set `revealed = true` and disconnect. Guard all `window`/`IntersectionObserver` access for SSR (no access during render; only inside effects).

    Create `src/components/Reveal.tsx` (default export) — a wrapper taking `children`, an optional `index`/`delay` for stagger, and an optional `as`/className passthrough. Render rules:
    - Default/SSR/first-render output: children wrapped in an element that is FULLY VISIBLE (opacity 1, no transform, no inline `opacity:0`, no `opacity-0` class).
    - Post-mount + motion-safe + not-yet-intersected: apply a `motion-safe`-gated pre-reveal state (e.g. classes that translate to opacity 0 + `translate-y-2` ONLY under `motion-safe:` and only once the hydrated guard is true), then transition to the revealed final state (opacity 1, translateY 0) over ~400ms ease-out when `revealed` flips. Apply the per-item stagger via an inline `transitionDelay` derived from `index` (e.g. `${index * 50}ms`) or an equivalent delay utility. Animate ONLY opacity + transform. Use `tw-animate-css`/Tailwind utilities already available; no new dependency.
    - FORBIDDEN: inline `style={{ opacity: 0 }}` on the SSR/first render; un-gated animation; animating layout properties; any markup that differs between server render and first client render (hydration mismatch).

    Create `Reveal.test.tsx` (@vitest-environment jsdom) covering the five behaviors above. Reuse the `MockIntersectionObserver` + `fireIntersection` pattern from `ProductGrid.test.tsx`. For the reduced-motion test, `vi.stubGlobal('matchMedia', ...)` returning `matches: true`. No semicolons, single quotes, trailing commas; `@/` alias imports.
  </action>
  <verify>
    <automated>cd /home/jeff/Documents/start-nest-shop/shop-front && node node_modules/vitest/vitest.mjs run src/components/Reveal.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `useReveal` is exported from `src/hooks/useReveal.ts` and accesses `window`/`IntersectionObserver`/`matchMedia` ONLY inside effects, never during render (SSR-safe) (source assertion).
    - The wrapper's initial render contains NO inline `style={{ opacity: 0 }}` and NO `opacity-0` class — fully visible SSR/no-JS baseline (test assertion).
    - Server-render markup and first client-render markup agree (no hydration mismatch): the hidden pre-reveal state is applied only after a mount guard flips (source + test assertion).
    - The pre-reveal hidden state is gated under `motion-safe:` and is skipped entirely when `prefers-reduced-motion: reduce` (test assertion via stubbed `matchMedia`).
    - On IntersectionObserver intersection, the element transitions to the revealed state (opacity 1 / translateY 0) over ~400ms (source assertion).
    - Only opacity + transform (translateY) are animated — no width/height/margin/top (source assertion).
    - Per-item stagger applied via `index`/`delay` (incremental `transitionDelay`) (test assertion).
    - Suite green via `node node_modules/vitest/vitest.mjs run` (NOT bun — known crash).
  </acceptance_criteria>
  <done>A reusable SSR-safe `useReveal` + `Reveal` wrapper exist: visible by default, motion-safe fade+rise post-hydration on viewport entry, reduced-motion respected, no hydration mismatch; suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Apply Reveal to grid cards + Featured/Trending/Categories rails (MOT-03)</name>
  <files>shop-front/src/components/catalog/ProductGrid.tsx, shop-front/src/components/feed/FeaturedRail.tsx, shop-front/src/components/feed/TrendingRail.tsx, shop-front/src/components/feed/CategoriesRail.tsx</files>
  <read_first>
    - shop-front/src/components/Reveal.tsx (the wrapper from Task 1 — its prop shape: children + stagger `index`/`delay`)
    - shop-front/src/components/catalog/ProductGrid.tsx (grid card map `products.map((product) => <ProductCard ... />)`; do NOT touch the `isPending` skeleton branch or the `grid-loading`/end-of-list/sentinel logic added in Plan 01)
    - shop-front/src/components/feed/FeaturedRail.tsx (rail item map: each item is a `<div key className="w-44 shrink-0"><ProductCard /></div>`)
    - shop-front/src/components/feed/TrendingRail.tsx (mirror of FeaturedRail)
    - shop-front/src/components/feed/CategoriesRail.tsx (CategoryCard pill items)
    - shop-front/src/components/catalog/ProductGrid.test.tsx, FeaturedRail.test.tsx, TrendingRail.test.tsx, CategoriesRail.test.tsx (these MUST still pass — reveal is additive; content stays present/visible in jsdom where matchMedia/IO are mocked)
    - .planning/phases/05-motion-loading-polish/05-UI-SPEC.md (Reveal-on-scroll contract)
  </read_first>
  <action>
    Wrap each visible item in the four consumers with the `Reveal` wrapper, passing the item's map `index` for the per-item stagger. In `ProductGrid.tsx`, wrap each rendered `ProductCard` in the success-state card map (NOT the skeleton placeholders, NOT the sentinel/loading rows). In `FeaturedRail.tsx` / `TrendingRail.tsx` / `CategoriesRail.tsx`, wrap each rail item (the existing `w-44 shrink-0` card div / CategoryCard) in `Reveal`, preserving each item's existing `key` on the outer element and keeping the fixed-width wrapper classes intact so horizontal sizing is unchanged.

    Because `Reveal` renders children fully visible by default, this must NOT change what renders in the existing tests (which mock `matchMedia`/`IntersectionObserver` or run without them) — content remains queryable. Verify all four consumers' existing tests still pass unchanged. Do not alter rail status/empty/error branches, the grid skeleton/end-of-list/loading branches, or any query wiring. No semicolons, single quotes, trailing commas; `@/` alias imports.
  </action>
  <verify>
    <automated>cd /home/jeff/Documents/start-nest-shop/shop-front && node node_modules/vitest/vitest.mjs run src/components/catalog/ProductGrid.test.tsx src/components/feed/FeaturedRail.test.tsx src/components/feed/TrendingRail.test.tsx src/components/feed/CategoriesRail.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Each rendered grid `ProductCard` (success state) is wrapped in `Reveal` with the map `index` passed for stagger (source assertion).
    - Each item in FeaturedRail, TrendingRail, and CategoriesRail is wrapped in `Reveal` with a stagger index, keeping existing `key` and `w-44 shrink-0` (or CategoryCard) wrapper classes (source assertion).
    - The grid skeleton (Plan 01), `grid-loading`, end-of-list, and rail status/empty/error branches are unchanged (source assertion).
    - All four consumers' existing test suites still pass — content remains visible/queryable in jsdom (test assertion).
    - No `opacity-0`/hidden default leaks into rendered content (carry-forward of the Task 1 SSR-safe invariant) (test assertion: items are found by their text in the success state).
    - Suite green via `node node_modules/vitest/vitest.mjs run` (NOT bun).
  </acceptance_criteria>
  <done>Grid cards and all three rails' items reveal-on-scroll with stagger after hydration, stay fully visible without JS / under reduced motion, existing suites green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server SSR render -> client hydration | The reveal wrapper sits at the SSR->hydration seam; a mismatch or a no-JS-hidden state would hide product content (availability/integrity of the rendered page) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Denial of Service (self-inflicted, availability) | `Reveal`/`useReveal` SSR baseline (MOT-03) | mitigate | Wrapper renders children fully visible by default; pre-reveal hidden state applied ONLY post-mount under a hydration guard + `motion-safe`. No inline `opacity:0` on SSR — content is never hidden from no-JS users or on hydration. Explicit no-hydration-mismatch acceptance criterion + test. |
| T-05-05 | Tampering | reveal markup | mitigate | Reveal adds only opacity/transform classes + a numeric `transitionDelay`; no `dangerouslySetInnerHTML`, no data-controlled HTML. Carry-forward Phase-4 XSS posture. |
| T-05-06 | Tampering | TanStack Start RC SSR integration | mitigate | All `window`/`matchMedia`/`IntersectionObserver` access confined to effects; no reliance on training-data API shapes — verify against installed `@tanstack/*` `.d.ts` / context7 before assuming render-context behavior (STATE.md blocker). |
| T-05-SC | Tampering | npm/bun installs | accept | No new packages installed; reveal uses existing Tailwind / `tw-animate-css`. No package-legitimacy gate required. |
</threat_model>

<verification>
- `node node_modules/vitest/vitest.mjs run` (full shop-front suite) green — Reveal is additive, no existing suite regresses.
- `bun --bun run check` passes for modified files (no-semicolons / single-quotes / trailing-commas).
- SSR/no-JS + hydration-mismatch + reduced-motion behavior is asserted in `Reveal.test.tsx` (Task 1). Live visual confirmation (actual fade+rise on scroll, no-JS fully-visible page) is covered by the Phase-level human-verify.
</verification>

<success_criteria>
- MOT-03: grid cards + all three rails reveal with a motion-safe fade+rise and per-item stagger after hydration.
- SSR/no-JS baseline: every item renders fully visible (opacity 1, no transform); no inline `opacity:0` on SSR.
- No hydration mismatch: server HTML and first client render agree.
- Reduced-motion users get no animation — final state immediately.
- Only opacity + transform animated; existing suites green via `node node_modules/vitest/vitest.mjs run`.
</success_criteria>

<output>
Create `.planning/phases/05-motion-loading-polish/05-02-SUMMARY.md` when done.
</output>
