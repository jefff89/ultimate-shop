---
phase: 04-composed-landing-feed
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - shop-front/src/components/feed/CategoryCard.tsx
  - shop-front/src/components/feed/CategoriesRail.tsx
  - shop-front/src/components/feed/CategoriesRail.test.tsx
  - shop-front/src/components/feed/LandingFeed.tsx
  - shop-front/src/routes/index.tsx
autonomous: true
requirements: [FEED-03, FEED-05]
user_setup: []

must_haves:
  truths:
    - "A Categories rail surfaces product categories in the composed feed, above the grid"
    - "The Categories rail fetches via its own useQuery with queryKey ['feed','categories'], isolated from the grid's ['catalog'] stream and the other rails"
    - "A failed or empty Categories rail shows a distinct error/empty state and does not crash the page or the other rails"
  artifacts:
    - path: "shop-front/src/components/feed/CategoriesRail.tsx"
      provides: "Categories rail bound to categoriesRailQueryOptions()"
    - path: "shop-front/src/components/feed/CategoryCard.tsx"
      provides: "Lean category chip/card (name, links by slug) — ProductCard is product-shaped, categories need their own item"
    - path: "shop-front/src/components/feed/LandingFeed.tsx"
      provides: "Composed feed now including all three rails"
  key_links:
    - from: "shop-front/src/components/feed/CategoriesRail.tsx"
      to: "categoriesRailQueryOptions()"
      via: "useQuery"
      pattern: "useQuery\\(categoriesRailQueryOptions\\(\\)\\)"
    - from: "shop-front/src/components/feed/LandingFeed.tsx"
      to: "CategoriesRail"
      via: "JSX render inside the feed"
      pattern: "<CategoriesRail"
---

<objective>
Add the Categories rail to the composed landing feed: a rail surfacing product categories
(name + slug), fetched on its own isolated query key, completing the four-section feed
(Hero + Featured + Trending + Categories) above the grid.

Purpose: A shopper sees a Categories rail in the homepage feed (FEED-03), fetched
independently so it never entangles the infinite cursor stream or the other rails (FEED-05).

Output: CategoryCard.tsx (a category-shaped item, since ProductCard is product-shaped),
CategoriesRail.tsx + test, the Categories rail added to LandingFeed.tsx, and a parallel
categories prefetch in the loader.
</objective>

<phase_goal>
**As a** shopper, **I want to** see a Categories rail in the homepage feed, **so that** I can jump into a product category instead of scrolling the entire catalog.
</phase_goal>

<execution_context>
@/home/jeff/Documents/start-nest-shop/.claude/get-shit-done/workflows/execute-plan.md
@/home/jeff/Documents/start-nest-shop/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-composed-landing-feed/04-01-PLAN.md
@.planning/phases/04-composed-landing-feed/04-01-SUMMARY.md
@.planning/phases/04-composed-landing-feed/04-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
| Symbol | File | Kind |
|--------|------|------|
| `CategoryCard` | shop-front/src/components/feed/CategoryCard.tsx | lean category item (escaped name; slug used for a link target) |
| `CategoriesRail` | shop-front/src/components/feed/CategoriesRail.tsx | useQuery(categoriesRailQueryOptions()) → Rail of CategoryCards |
| LandingFeed update | shop-front/src/components/feed/LandingFeed.tsx | renders `<CategoriesRail/>` (final rail) |
| loader update | shop-front/src/routes/index.tsx | parallel prefetchQuery(categoriesRailQueryOptions()) |

Consumes from Plan 01: `categoriesRailQueryOptions()`, `fetchCategories` (seam), `CategoryRailItem` shared type, the shared `Rail` shell.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CategoryCard + CategoriesRail + compose into the feed</name>
  <files>shop-front/src/components/feed/CategoryCard.tsx, shop-front/src/components/feed/CategoriesRail.tsx, shop-front/src/components/feed/CategoriesRail.test.tsx, shop-front/src/components/feed/LandingFeed.tsx, shop-front/src/routes/index.tsx</files>
  <read_first>
    - shop-front/src/components/feed/Rail.tsx (shared shell: loading/empty/error/success — reuse, do not fork)
    - shop-front/src/components/feed/FeaturedRail.tsx (rail wiring pattern to mirror)
    - shop-front/src/components/feed/TrendingRail.tsx (Plan 02 — second example of the pattern)
    - shop-front/src/components/feed/LandingFeed.tsx (add CategoriesRail as the final rail)
    - shop-front/src/data/feed.query.ts (categoriesRailQueryOptions, key ['feed','categories'])
    - shared/catalog.contract.ts (CategoryRailItem shape: id, name, slug — added by Plan 01)
    - shop-front/src/routes/index.tsx (loader Promise.all — add categories prefetch)
  </read_first>
  <behavior>
    - CategoryCard renders the category name as escaped JSX (no raw HTML) and uses slug for a link target (a TanStack Router <Link> or an anchor; no category route exists yet so link to "/" or a slug query — keep it non-crashing).
    - CategoriesRail uses useQuery(categoriesRailQueryOptions()); queryKey is ['feed','categories'], not ['catalog'] / ['feed','featured'] / ['feed','trending'].
    - success-with-items renders one CategoryCard per category; status pending/error/empty render the Rail's distinct states; error does not throw.
    - LandingFeed renders Hero, Featured, Trending, then Categories.
  </behavior>
  <action>
    Create CategoryCard.tsx taking a `category: CategoryRailItem`: render the `name` as escaped JSX children inside a chip/card (Tailwind new-york/zinc, e.g. a rounded pill `shrink-0 rounded-full border px-4 py-2`). Use `slug` for the link target. ProductCard is intentionally NOT reused here — it is product-shaped (price/rating/image); a category needs its own lean item. If a category image is ever introduced it MUST go through SafeImage; for now keep it text-only to avoid an untrusted URL.

    Create CategoriesRail.tsx mirroring the rail pattern: `const { data, status } = useQuery(categoriesRailQueryOptions())`, `items = data ?? []`, return `<Rail title="Categories" status={status}>` mapping items to `<CategoryCard>`.

    Modify LandingFeed.tsx to render `<CategoriesRail/>` after `<TrendingRail/>` (final order: Hero, Featured, Trending, Categories).

    Modify index.tsx: add `context.queryClient.prefetchQuery(categoriesRailQueryOptions())` to the existing loader `Promise.all([...])` so all rails prefetch in parallel with the catalog (non-throwing prefetch; a categories failure degrades to the Rail error state, never rejects the loader). Do NOT edit routeTree.gen.ts.
  </action>
  <verify>
    <automated>cd /home/jeff/Documents/start-nest-shop/shop-front && node node_modules/vitest/vitest.mjs run src/components/feed/CategoriesRail.test.tsx && bun --bun run build</automated>
  </verify>
  <acceptance_criteria>
    - CategoriesRail.test.tsx asserts the success path renders one CategoryCard per category and each category name appears as text.
    - Test asserts a rejecting categories queryFn renders the Rail error state (role="alert") without throwing.
    - Test asserts status==='success' with an empty array renders the Rail empty state (distinct from error).
    - Source assertion: CategoriesRail.tsx calls `useQuery(categoriesRailQueryOptions())` (grep); no reference to `['catalog']` or the other rail factories.
    - Source assertion: LandingFeed.tsx contains `<FeaturedRail`, `<TrendingRail`, and `<CategoriesRail` in that order.
    - `bun --bun run build` succeeds and the full vitest suite (including the Phase 3 grid exactly-once test) stays green.
  </acceptance_criteria>
  <done>The composed feed shows Hero + Featured + Trending + Categories above the grid; Categories fetches on ['feed','categories'] with distinct loading/empty/error states; build green and CategoriesRail.test.tsx passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mock seam → Categories rail UI | Category data (name, slug) flows into rail JSX and link targets |
| categories query → loader/page | A categories fetch can fail; must not take down route, other rails, or grid |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Tampering/XSS | Category name rendering (CategoryCard.tsx) | mitigate | Render category name only as escaped JSX children; no dangerouslySetInnerHTML |
| T-04-11 | Tampering/Open-redirect | Category slug used as a link target | mitigate | slug is used only as a path segment / query value (not an external href); CategoryRailItem.slug is contract-validated (z.string()) at the seam; no raw user URL navigation |
| T-04-12 | Information disclosure | Categories rail error state | mitigate | Inherits Rail error branch: fixed generic copy, no error/stack interpolation |
| T-04-13 | Denial of service | A failing categories rail crashing page or disturbing other rails/grid | mitigate | Independent `['feed','categories']` useQuery; fail-closed Rail error branch; non-throwing `prefetchQuery`; shares no state with `['catalog']` or the other rails (FEED-05) |
| T-04-14 | Denial of service | Unbounded categories payload | mitigate | fetchCategories returns a fixed small seeded set (Plan 01) |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies; if any install is introduced, halt and run legitimacy gate |
</threat_model>

<verification>
- Full vitest run green; grid exactly-once-per-boundary test still passes.
- `bun --bun run build` succeeds.
- Grep: CategoriesRail.tsx uses `categoriesRailQueryOptions`; LandingFeed renders all three rails in order; no rail file references `CATALOG_QUERY_KEY`.
</verification>

<success_criteria>
- Categories rail surfaces product categories in the feed above the grid (FEED-03).
- Categories rail fetches independently on ['feed','categories'], isolated from the grid and the other rails (FEED-05).
- Categories rail has distinct loading/empty/error states; failure stays isolated to the rail.
- The full four-section feed (Hero + Featured + Trending + Categories) renders above the working infinite grid.
</success_criteria>

<output>
Create `.planning/phases/04-composed-landing-feed/04-03-SUMMARY.md` when done.
</output>
