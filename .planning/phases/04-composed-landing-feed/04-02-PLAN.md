---
phase: 04-composed-landing-feed
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - shop-front/src/components/feed/TrendingRail.tsx
  - shop-front/src/components/feed/TrendingRail.test.tsx
  - shop-front/src/components/feed/LandingFeed.tsx
  - shop-front/src/routes/index.tsx
autonomous: true
requirements: [FEED-04, FEED-05]
user_setup: []

must_haves:
  truths:
    - "A Trending rail shows isTrending products in the composed feed, above the grid"
    - "The Trending rail fetches via its own useQuery with queryKey ['feed','trending'], isolated from the grid's ['catalog'] stream and from the Featured rail"
    - "A failed or empty Trending rail shows a distinct error/empty state and does not crash the page, the Featured rail, or the grid"
  artifacts:
    - path: "shop-front/src/components/feed/TrendingRail.tsx"
      provides: "Trending rail bound to trendingRailQueryOptions()"
    - path: "shop-front/src/components/feed/LandingFeed.tsx"
      provides: "Composed feed now including the Trending rail"
  key_links:
    - from: "shop-front/src/components/feed/TrendingRail.tsx"
      to: "trendingRailQueryOptions()"
      via: "useQuery"
      pattern: "useQuery\\(trendingRailQueryOptions\\(\\)\\)"
    - from: "shop-front/src/components/feed/LandingFeed.tsx"
      to: "TrendingRail"
      via: "JSX render inside the feed"
      pattern: "<TrendingRail"
---

<objective>
Add the Trending rail to the composed landing feed: a rail rendering `isTrending` products,
fetched on its own isolated query key, slotted into LandingFeed above the grid.

Purpose: A shopper sees a Trending rail alongside Featured in the homepage feed (FEED-04),
each on an independent query so one rail's loading/error never disturbs the others or the
infinite cursor stream (FEED-05).

Output: TrendingRail.tsx, its test, the Trending rail added to LandingFeed.tsx, and an
additional parallel rail prefetch in the route loader.
</objective>

<phase_goal>
**As a** shopper, **I want to** see a Trending rail in the homepage feed, **so that** I can discover what is currently popular without scrolling the full catalog.
</phase_goal>

<execution_context>
@/home/jeff/Documents/start-nest-shop/.claude/get-shit-done/workflows/execute-plan.md
@/home/jeff/Documents/start-nest-shop/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-composed-landing-feed/04-01-PLAN.md
@.planning/phases/04-composed-landing-feed/04-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
| Symbol | File | Kind |
|--------|------|------|
| `TrendingRail` | shop-front/src/components/feed/TrendingRail.tsx | useQuery(trendingRailQueryOptions()) → Rail of ProductCards |
| LandingFeed update | shop-front/src/components/feed/LandingFeed.tsx | renders `<TrendingRail/>` after `<FeaturedRail/>` |
| loader update | shop-front/src/routes/index.tsx | parallel prefetchQuery(trendingRailQueryOptions()) |

Consumes from Plan 01: `trendingRailQueryOptions()`, `fetchTrendingProducts` (seam), the shared `Rail` shell, `ProductCard`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TrendingRail component + compose into the feed</name>
  <files>shop-front/src/components/feed/TrendingRail.tsx, shop-front/src/components/feed/TrendingRail.test.tsx, shop-front/src/components/feed/LandingFeed.tsx, shop-front/src/routes/index.tsx</files>
  <read_first>
    - shop-front/src/components/feed/FeaturedRail.tsx (Plan 01 — mirror this exactly, swapping featured→trending; same Rail shell usage)
    - shop-front/src/components/feed/Rail.tsx (the shared shell with loading/empty/error/success states)
    - shop-front/src/components/feed/LandingFeed.tsx (add TrendingRail after FeaturedRail)
    - shop-front/src/data/feed.query.ts (trendingRailQueryOptions, key ['feed','trending'])
    - shop-front/src/components/feed/FeaturedRail.test.tsx (mirror its test structure)
    - shop-front/src/routes/index.tsx (loader prefetch Promise.all — add the trending rail)
    - shop-front/src/components/catalog/ProductCard.tsx (rail item card to reuse)
  </read_first>
  <behavior>
    - TrendingRail uses useQuery(trendingRailQueryOptions()); its queryKey is ['feed','trending'], not ['catalog'] and not ['feed','featured'].
    - success-with-items renders one ProductCard per trending item (all isTrending).
    - status==='pending' → Rail loading state; status==='error' → Rail error state (role="alert", no raw error), does not throw; status==='success' && 0 items → Rail empty state.
    - LandingFeed renders FeaturedRail then TrendingRail, both above the grid.
  </behavior>
  <action>
    Create TrendingRail.tsx mirroring FeaturedRail.tsx: `const { data, status } = useQuery(trendingRailQueryOptions())`, `items = data ?? []`, return `<Rail title="Trending" status={status}>` mapping items to width-wrapped `<ProductCard>`. Do NOT introduce a second rail-shell abstraction — reuse the Plan 01 `Rail`.

    Modify LandingFeed.tsx to render `<TrendingRail/>` immediately after `<FeaturedRail/>` (order: Hero, Featured, Trending; Categories slots in via Plan 03).

    Modify index.tsx: add `context.queryClient.prefetchQuery(trendingRailQueryOptions())` into the existing `Promise.all([...])` in the loader so the trending rail is prefetched in parallel with the catalog and featured prefetches (no waterfall, non-throwing prefetch so a trending fetch failure degrades to the Rail error state and never rejects the loader). Do NOT edit routeTree.gen.ts.
  </action>
  <verify>
    <automated>cd /home/jeff/Documents/start-nest-shop/shop-front && node node_modules/vitest/vitest.mjs run src/components/feed/TrendingRail.test.tsx && bun --bun run build</automated>
  </verify>
  <acceptance_criteria>
    - TrendingRail.test.tsx asserts the success path renders one ProductCard per trending item.
    - Test asserts a rejecting trending queryFn renders the Rail error state (role="alert") without throwing and without affecting a sibling-rendered element (failure is isolated).
    - Source assertion: TrendingRail.tsx calls `useQuery(trendingRailQueryOptions())` (grep), and references no `['catalog']` / `catalogInfiniteQueryOptions` / `featuredRailQueryOptions`.
    - Source assertion: LandingFeed.tsx contains both `<FeaturedRail` and `<TrendingRail`, with Trending after Featured.
    - `bun --bun run build` succeeds and the full vitest suite stays green (grid exactly-once test unaffected).
  </acceptance_criteria>
  <done>The composed feed shows Featured then Trending rails above the grid; Trending fetches on ['feed','trending'] with distinct loading/empty/error states; build green and TrendingRail.test.tsx passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mock seam → Trending rail UI | Trending product data (names, image URLs) flows into rail JSX |
| trending query → loader/page | A trending fetch can fail; must not take down route, other rails, or grid |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Tampering/XSS | Trending item name/image rendering | mitigate | Reuses ProductCard (SafeImage + escaped JSX text); no new render path, no dangerouslySetInnerHTML |
| T-04-07 | Information disclosure | Trending rail error state | mitigate | Inherits Rail error branch: fixed generic copy, no error/stack interpolation |
| T-04-08 | Denial of service | A failing trending rail crashing page or disturbing grid/featured | mitigate | Independent `['feed','trending']` useQuery; fail-closed Rail error branch (no throw); non-throwing `prefetchQuery` in loader; shares no state with `['catalog']` or `['feed','featured']` (FEED-05) |
| T-04-09 | Denial of service | Unbounded trending payload | mitigate | RAIL_LIMIT (Plan 01) caps fetchTrendingProducts length |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies; if any install is introduced, halt and run legitimacy gate |
</threat_model>

<verification>
- Full vitest run green; grid exactly-once-per-boundary test still passes (rail did not touch grid state).
- `bun --bun run build` succeeds.
- Grep: TrendingRail.tsx uses `trendingRailQueryOptions`; no reference to `['catalog']`.
</verification>

<success_criteria>
- Trending rail displays isTrending products in the feed above the grid (FEED-04).
- Trending rail fetches independently on ['feed','trending'], isolated from the grid and the Featured rail (FEED-05).
- Trending rail has distinct loading/empty/error states; failure stays isolated to the rail.
</success_criteria>

<output>
Create `.planning/phases/04-composed-landing-feed/04-02-SUMMARY.md` when done.
</output>
